/**
 * 一次性收入的會計科目（`orders.fee_type`）。
 *
 * 【保證金只在「一次性收入」出現】（2026-08-13 加入）
 * 見底下的 ONEOFF_FEE_TYPES —— 加費、契約固定加費、定期收費都選不到它。
 *
 * 【保證金跟「押金」不是同一件事】
 * 押金走 deposits 那張表，收了要退，**不是營收**。
 * 保證金是收了就認列的收入（違約沒收、設備保證金轉收入之類），
 * 所以它是一個會計科目而不是一筆押金。
 *
 * 兩者名字很像而性質相反，填錯的方向也很明確：
 * 把該退的錢記成營收，那個月的數字會憑空多出來而且不會有人發現。
 *
 * 【為什麼要有這支】
 * 這份清單原本各寫各的:契約頁的「加費」有八種,短租訂單的「加費」只有三種
 * （清潔費／修繕費／其他）,而「新增一次性收入」根本沒有這個欄位 ——
 * 只能寫在備註裡。同一件事三種寫法,營收報表按 fee_type 分組時就會看到
 * 一堆自由文字混在一起。
 *
 * 全站都從這裡取,要增減科目只改這一行。
 *
 * 【順序】
 * 常用的排前面,「其他」永遠最後 —— 它是保底,不是一個真的分類。
 *
 * 【取消相關的收入歸在「其他」】
 * Airbnb 取消收入、取消預訂這幾種(2026-08 時 73 筆、約 150 萬)刻意不另立科目,
 * 明細留在備註裡。這是業務上的決定,不是漏掉。
 *
 * 代價要知道:營收報表的「其他」會包含這筆錢,光看報表分不出組成,
 * 得回訂單看備註。之後若要在報表上分開,加一個科目再重新標記即可 ——
 * 資料都還在,不是不可逆的。
 */
export const FEE_TYPES = [
  /*
   * 【水電瓦斯 vs 水費／電費／瓦斯費】（2026-08-19）
   *
   * `account_codes`（收支共用的會計科目表）裡只有一個 `utility 水電瓦斯` ——
   * 水、電、瓦斯在會計上是同一格。而這份硬寫的清單把它拆成三個,
   * 兩邊對不起來。
   *
   * **這裡加上「水電瓦斯」而不是拿掉那三個** —— 舊資料還在用
   * （水費 2 張契約、網路費 3 張）,拿掉會讓那幾筆的科目變成選不到的值。
   *
   * 代價要知道:同一筆電費可能被記成「電費」也可能是「水電瓦斯」,
   * 而營收報表分不出那是同一件事。新的一律走固定加費的預設值
   * （CONTRACT_FEE_PRESETS 已經指定水電瓦斯）,舊的等哪天一起遷移。
   */
  '水電瓦斯',
  '水費', '電費', '網路費', '瓦斯費', '管理費',
  '停車費', '設備費', '清潔費', '修繕費',
  /*
   * 運費（2026-08-19 使用者指定「項目 運費 會計科目 運費」）。
   *
   * 資料庫那邊同步加了 `freight` 科目與對照（migration_148）——
   * **兩邊要一起加**。只加這裡的話，那筆收入在營收報表上會掉進「其他」,
   * 而金額是對的、名目也是對的，只有分組錯了 —— 沒有人會發現。
   */
  '運費',
  '其他',
] as const;

/**
 * 只有「一次性收入」才選得到的科目。
 *
 * 【為什麼要跟 FEE_TYPES 分開】
 * FEE_TYPES 那份是**會重複發生的費用**：加費、契約固定加費、
 * 定期收費每個月自動長出來的那些。保證金不是那種東西 ——
 * 它是一次性的事件（違約沒收、履約保證金轉列收入）。
 *
 * 混在同一份清單裡的話，它會出現在「每月自動產生」的選單上，
 * 而一個每月自動產生的保證金在會計上講不通 ——
 * 那個選項存在本身就是在邀請人填錯。
 */
export const ONEOFF_ONLY_FEE_TYPES = ['保證金'] as const;

/**
 * 一次性收入的科目清單 = 共用的那些 ＋ 只有它有的。
 * 「其他」永遠排最後 —— 它是保底，不是一個真的分類。
 */
export const ONEOFF_FEE_TYPES = [
  ...FEE_TYPES.filter((t) => t !== '其他'),
  ...ONEOFF_ONLY_FEE_TYPES,
  '其他',
] as const;

export type FeeType = (typeof FEE_TYPES)[number];

/**
 * 契約固定加費的預設項目。
 *
 * 【為什麼「設備費-冰箱」要拆成兩欄】
 * 使用者說的是一句「設備費-冰箱」，但系統裡科目與項目是分開的兩欄:
 * 營收報表按**科目**分組，項目是底下的細目。
 *
 * 合成一個字串塞進 fee_type 的話，報表上會出現「設備費-冰箱」「設備費-電視」
 * 兩個各自獨立的科目，永遠回答不了「設備費一共收多少」。
 *
 * 拆開之後兩個問題都答得出來:設備費合計看科目，是冰箱還是電視看項目。
 * 畫面上仍然是一個下拉選五項，選完自動填好兩欄 —— 使用者感覺不到差別。
 */
export const CONTRACT_FEE_PRESETS: { label: string; fee_type: string; item_name: string | null }[] = [
  { label: '管理費',        fee_type: '管理費', item_name: null },
  /*
   * 【電費／飲用水／其它】（2026-08-19 使用者指定）
   *
   *     項目：電費   → 會計科目：水電瓦斯
   *     項目：飲用水 → 會計科目：管理費
   *     項目：其它   → 會計科目：其它
   *
   * 三個都是「科目已經有了，缺的是項目」——
   * 所以只加預設值，不動 FEE_TYPES 也不動資料庫。
   *
   * 【為什麼電費的科目是「水電瓦斯」而不是「電費」】
   *
   * `account_codes` 那張表（收支共用的會計科目）裡就是 `utility 水電瓦斯`,
   * 沒有「電費」這個科目 —— 水、電、瓦斯在會計上是同一格。
   *
   * 而 FEE_TYPES 這份硬寫的清單把它拆成「水費／電費／瓦斯費」三個,
   * 跟資料庫那張表對不起來。**這裡先照使用者要的歸類走**,
   * 兩份清單要不要合併是另一件事（見底下 FEE_TYPES 的註解）。
   */
  { label: '電費',          fee_type: '水電瓦斯', item_name: '電費' },
  { label: '飲用水',        fee_type: '管理費',   item_name: '飲用水' },
  { label: '停車費',        fee_type: '停車費', item_name: null },
  // 網路費本來就在 FEE_TYPES 裡（營收科目對到 internet，見 migration_91），
  // 只是沒放進固定加費的預設清單 —— 加在這裡就好，不用動資料庫。
  { label: '網路費',        fee_type: '網路費', item_name: null },
  { label: '水費',          fee_type: '水費',   item_name: null },
  // 垃圾代收歸在清潔費底下（使用者指定）—— 科目看清潔費，
  // 要單獨知道垃圾代收收了多少就看項目。另立科目要改資料庫的科目對應表，
  // 為一個小金額的細目不值得。
  /*
   * 【清潔費與修繕費要留一個「沒有項目」的選項】（2026-08-19）
   *
   * 改成用 presets 之前,這兩個科目在下拉裡是選得到的。
   * 只留「垃圾代收」的話,想記一筆純清潔費就沒有選項可選 ——
   * **那是功能倒退,而且不會報錯**,只會讓人退而選一個最像的。
   */
  { label: '清潔費',        fee_type: '清潔費', item_name: null },
  { label: '垃圾代收',      fee_type: '清潔費', item_name: '垃圾代收' },
  { label: '修繕費',        fee_type: '修繕費', item_name: null },
  { label: '設備費－冰箱',   fee_type: '設備費', item_name: '冰箱' },
  { label: '設備費－洗烘衣機', fee_type: '設備費', item_name: '洗烘衣機' },
  { label: '設備費－電視',   fee_type: '設備費', item_name: '電視' },
  /*
   * 運費。項目與科目同名，所以 item_name 留 null ——
   * 跟管理費、停車費、水費那幾個一樣的作法。
   * 填成「運費－運費」的話報表上會多一層沒有意義的細目。
   */
  { label: '運費',          fee_type: '運費',   item_name: null },
  // 「其它」永遠排最後 —— 它是保底,不是一個真的分類
  { label: '其它',          fee_type: '其他',   item_name: '其它' },
];

/** 由科目＋項目反查預設的顯示名稱。找不到就自己組，不要顯示成空白。 */
export function feeLabel(fee_type: string | null, item_name: string | null): string {
  const hit = CONTRACT_FEE_PRESETS.find(
    (p) => p.fee_type === fee_type && (p.item_name ?? null) === (item_name || null));
  if (hit) return hit.label;
  return item_name ? `${fee_type ?? '其他'}－${item_name}` : (fee_type ?? '其他');
}

/**
 * 沒選科目時記到認列表的預設值。
 * 跟 migration_75 裡的預設一致 —— 兩邊不同步的話,同一種單會出現兩個科目。
 */
export const FEE_DEFAULT = '其他';


/**
 * 一次性費用的預設項目（2026-08-19 使用者指定「固定加費 一次性費用都加入喔」）。
 *
 * = 固定加費那份 ＋ 保證金。
 *
 * **兩邊共用同一份清單是刻意的** —— 分開寫的話,
 * 加一個項目要記得改兩個地方,而漏掉的那邊不會報錯,
 * 只會讓同一種費用在兩張表裡歸到不同的科目。
 * 那正是 FEE_TYPES 這支檔案當初要解決的問題。
 *
 * 保證金只在這裡出現:它是一次性的事件（違約沒收、履約保證金轉列收入）,
 * 不會每個月自動長出來。
 */
export const ONEOFF_PRESETS: { label: string; fee_type: string; item_name: string | null }[] = [
  ...CONTRACT_FEE_PRESETS.filter((p) => p.label !== '其它'),
  { label: '保證金', fee_type: '保證金', item_name: null },
  { label: '其它',   fee_type: '其他',   item_name: '其它' },
];

/**
 * 由 label 反查科目與項目。找不到回 null —— **不要猜一個最像的**。
 *
 * 猜錯的代價是那筆收入被歸到別的科目,而營收報表上看不出來。
 */
export function presetOf(
  label: string,
  list: { label: string; fee_type: string; item_name: string | null }[] = ONEOFF_PRESETS,
): { fee_type: string; item_name: string | null } | null {
  const hit = list.find((p) => p.label === label);
  return hit ? { fee_type: hit.fee_type, item_name: hit.item_name } : null;
}

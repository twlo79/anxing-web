/**
 * 側邊選單的圖示。
 *
 * ============================================================
 * 【為什麼不用 emoji】
 *
 * 原本是 🕐 🏨 📋 💰 🧾 這一類。emoji 的問題不是「不好看」，是三件事：
 *
 *   1. **每個系統長得不一樣。** 同一個 🏨 在 Windows、iPhone、Android
 *      是三套完全不同的插畫，粗細、顏色、風格都對不起來。
 *      畫面在自己電腦上看起來還行，在別人手機上就是拼貼。
 *
 *   2. **選取時看不出狀態。** 選中的那一列底色是藍的、文字轉白，
 *      但 emoji 永遠是彩色的 —— 它不會跟著變，看起來像貼上去的貼紙。
 *
 *   3. **語意很勉強。** 🧾 是收據、📒 是筆記本、⚙️ 是齒輪 ——
 *      能挑的字符就那些，最後是「有一個大概像的就用」。
 *
 * 改成單色線條圖示（stroke = currentColor）之後：
 * 粗細統一、選取時跟著文字一起變白、而且可以畫出真正對應的形狀。
 *
 * ============================================================
 * 【為什麼是自己畫，不是裝一個圖示套件】
 *
 * 這裡只需要 14 個圖示。裝 lucide-react 是為了 1,500 個裡的 14 個，
 * 而且 icon 套件的 tree-shaking 常常沒有想像中乾淨。
 * 這個檔案是純 SVG path，沒有相依、沒有打包成本。
 *
 * 統一規格：24×24 viewBox、stroke-width 1.75、圓端點。
 * 全部用 stroke 不用 fill —— 混用的話同一排看起來會有的重有的輕。
 */

export type IconName =
  | 'clock' | 'bed' | 'contract' | 'coins' | 'receipt' | 'lock' | 'book'
  | 'chart' | 'broom' | 'user' | 'star' | 'sparkle' | 'bell' | 'settings';

/** 每個圖示的 path。d 用陣列是為了多筆 path 的情況。 */
const PATHS: Record<IconName, string[]> = {
  // 出勤：時鐘
  clock: ['M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18Z', 'M12 7v5l3 2'],
  // 訂單｜收入：床（短租）
  bed: ['M3 18v-8h13a4 4 0 0 1 4 4v4', 'M3 18v2', 'M21 18v2', 'M3 14h18', 'M7 10V7h5v3'],
  // 契約｜收入：有折角的文件
  contract: ['M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8l-5-5Z',
             'M14 3v5h5', 'M9 13h6', 'M9 17h4'],
  // 營收表：硬幣
  coins: ['M9 14a5 5 0 1 0 0-10 5 5 0 0 0 0 10Z',
          'M14.9 6.2A5 5 0 1 1 15 19.8', 'M9 20h6'],
  // 請款單控管：收據
  receipt: ['M6 3h12v18l-2-1.4-2 1.4-2-1.4-2 1.4-2-1.4L6 21V3Z', 'M9.5 8h5', 'M9.5 12h5'],
  // 押金管理：鎖
  lock: ['M5 11h14v10H5V11Z', 'M8 11V7a4 4 0 0 1 8 0v4', 'M12 15v2'],
  // 支出明細：帳簿
  book: ['M5 4h13a1 1 0 0 1 1 1v15H6a1 1 0 0 1-1-1V4Z', 'M5 17h14', 'M9 8h6', 'M9 12h6'],
  // 財務儀錶板：長條圖
  chart: ['M4 20h16', 'M7 20v-6', 'M12 20V8', 'M17 20v-9'],
  // 房務管理：掃把
  broom: ['M14 5l5 5', 'M13.5 6.5 7 13l4 4 6.5-6.5', 'M7 13l-3 6 6-3', 'M4 19l2.5.5'],
  // 客戶管理：人
  user: ['M12 12a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z', 'M4.5 20a7.5 7.5 0 0 1 15 0'],
  // 房源評價：星
  star: ['M12 3.5l2.6 5.4 5.9.8-4.3 4.1 1.1 5.9-5.3-2.9-5.3 2.9 1.1-5.9L3.5 9.7l5.9-.8L12 3.5Z'],
  // 清潔記錄：閃亮
  sparkle: ['M12 3l1.7 4.8L18.5 9.5l-4.8 1.7L12 16l-1.7-4.8L5.5 9.5l4.8-1.7L12 3Z',
            'M18 16l.7 2 2 .7-2 .7-.7 2-.7-2-2-.7 2-.7.7-2Z'],
  // 通知設定：鈴鐺
  bell: ['M6 9a6 6 0 0 1 12 0c0 4 1.5 5.5 1.5 5.5h-15S6 13 6 9Z', 'M10 18a2 2 0 0 0 4 0'],
  // 權限管理：齒輪
  settings: ['M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z',
             'M19.4 14.5a1.6 1.6 0 0 0 .3 1.8l.1.1a2 2 0 1 1-2.8 2.8l-.1-.1a1.6 1.6 0 0 0-1.8-.3 1.6 1.6 0 0 0-1 1.5v.2a2 2 0 1 1-4 0v-.1a1.6 1.6 0 0 0-1-1.5 1.6 1.6 0 0 0-1.8.3l-.1.1a2 2 0 1 1-2.8-2.8l.1-.1a1.6 1.6 0 0 0 .3-1.8 1.6 1.6 0 0 0-1.5-1H3a2 2 0 1 1 0-4h.1a1.6 1.6 0 0 0 1.5-1 1.6 1.6 0 0 0-.3-1.8l-.1-.1a2 2 0 1 1 2.8-2.8l.1.1a1.6 1.6 0 0 0 1.8.3H9a1.6 1.6 0 0 0 1-1.5V3a2 2 0 1 1 4 0v.1a1.6 1.6 0 0 0 1 1.5 1.6 1.6 0 0 0 1.8-.3l.1-.1a2 2 0 1 1 2.8 2.8l-.1.1a1.6 1.6 0 0 0-.3 1.8V9a1.6 1.6 0 0 0 1.5 1h.2a2 2 0 1 1 0 4H21a1.6 1.6 0 0 0-1.5 1Z'],
};

export default function NavIcon({ name, className = '' }: { name: IconName; className?: string }) {
  return (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={1.75}
      strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"
      // 18px：跟 15px 的中文並排時，比文字略大一點點才不會顯得縮水
      className={`w-[18px] h-[18px] shrink-0 ${className}`}>
      {PATHS[name].map((d, i) => <path key={i} d={d} />)}
    </svg>
  );
}

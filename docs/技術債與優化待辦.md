# 技術債與優化待辦

> 2026-09-23 起。**這是活的清單**：做完劃掉、新的加進來，每一項都要有「什麼時候做」。
> 沒有觸發條件的項目不列 —— 那種東西列了只會一直躺在這裡。
>
> 來龍去脈：`README.md` 第 8.2 節（做完的）、專案文件 `架構體檢-2026-09-22.md`（為什麼）。

## 待辦（有觸發條件才做）

| # | 項目 | 什麼時候做 | 為什麼現在不做 | 大概多大 |
|---|---|---|---|---|
| 1 | **面板裡的訊息改到動作旁邊**（`deposits` 39 處、`purchases` 31 處）| 錯誤改紅字（2026-09-23）上線兩三週後**還有人說「按了沒反應」** | 錯誤現在是紅色、固定在畫面上方、留到按掉 —— 「看不到」那一半解掉了，剩下的是體驗。要一輪出稿過審 ＋ 70 個呼叫點各自留位置 | 一頁一輪，要過審 |
| 2 | **資料庫結構債**：三組重複唯一索引（`expenses(source_item_id)`、`expenses(fee_request_id)`、`orders(order_key)` 各兩支）、十條外鍵指到 `auth.users` 而非 `profiles`、`staff.role` 與 `profiles.role` 兩份、`properties_no_cycle` 與 `properties_parent_guard` 重疊、6 張沒人讀的備份表（`orders_backup_136` 2,681 列…）| **下一次要改 schema 時一起** | 現在沒事，單獨開工只有風險沒有收益 | 一支 migration |
| 3 | **`expenses` 頁整本撈改 `fetchAll()`**（`expenses/page.tsx` 的 `load()`）| 改到支出頁時順手，**或監控 #1 到門檻** | 現在 419 筆，離 1,000 有距離 | 兩行 |
| 4 | **剩下 69 處 `update`／`delete` 沒接 `.select('id')`** | 改到那一頁時順手，用 `lib/write-guard.ts` 的 `writeError()` | 它們寫完就重新載入，失敗畫面會自己說實話；補了只是多 69 行一樣的東西 | 每處 2 行 |
| 5 | **`schema_migrations` 補記**（238、255 ~ 259 跑過但沒紀錄）| 下一支 migration 順手 | — | 補記要帶證據：`insert … where exists(那個痕跡)`，不要無條件寫（README 9.4）|
| 6 | **11 份元件內的 `flash()`**（`ContractFees`、`DepositPayments`、`OrderPayments`…）改走 `useFlash()` | 改到那個元件時順手 | 它們把訊息畫在面板內，不是「跳在畫面外」那個問題 | 每份 3 行 |
| 7 | **`leave_types.has_quota` 沒有畫面可以切**（現在靠 SQL）| 下一次動「管理 → 假別額度」那頁時加一個勾勾 | 一年動一次的設定，SQL 撐得住 | 半輪，要過審 |
| 8 | **`contracts.monthly_rent` 退役**：它是 `round(每期 ÷ 月數)` 的推導值卻存成欄位，列表與抽屜的「月 $X」都拿它 | 下一次動 `contracts` 表時一起（併進 #2）| 298 之後金額真相是 `amount_per_period`，這欄只剩顯示用；現在沒事 | 半支 migration ＋ 3 處顯示改 `lib/period-split` |
| 9 | **`anon` 對 public 每張表都有 select**（Supabase 舊預設）| 下一次動 policy 時一起 | RLS 全開、未登入沒有 `auth.uid()` 一律擋掉，現在不漏；收掉是保險 —— 萬一哪天有人寫 `using (true)`。收之前 grep 一次沒有任何地方用 anon key 不帶 JWT 讀表（09-24 看過：登入頁與 API route 都不是）| 一句 `revoke select on all tables in schema public from anon` ＋ 自檢 |
| — | `key={i}` 34 處、`any` 235 處（`contracts` 84）| **不做** | 改了看不出好處但可能改壞 | — |

## 監控（定期看一眼，到了門檻就動）

| # | 看什麼 | 怎麼看 | 門檻 | 到了要做什麼 |
|---|---|---|---|---|
| 1 | **`expenses` 筆數** | SQL Editor：`select count(*) from expenses;` | **800** | 做待辦 #3。到 1,000 之後支出頁會**安靜少算** —— 總額看起來正常，只有月底對不起來 |
| 2 | **刪除延展時出現橘字「沒有留底」** | 契約頁按刪除延展，看訊息 | 出現就算 | 那張契約是 09-23 之前展延的，迄日是用月底猜的 —— **手動核對迄日**。之後展延的都有留底，不會再出現 |
| 3 | **有人回報「按了沒反應」** | 口頭 | 一次 | 先確認不是列數檢查或 1,000 列上限的訊息；是的話做待辦 #1 |
| 4 | **Console 的 React #418／#423／#425** | F12 | 一直有 | **不用管** —— Grammarly 擴充套件改了 `<body>`，乾淨瀏覽器沒有。要確認就停用 Grammarly 重整 |
| 5 | **新頁面有沒有自己寫 `flash()`** | `grep -rn "function flash" src` | 多出一份 | 改成 `useFlash()`。同一件事不要有第二種寫法 |
| 6 | **新頁面有沒有自己寫 `fmt`／`money`** | `grep -rnE "const (fmt\|money) =" src` | 多出一份 | 改成 `import { fmtInt as fmt } from '@/lib/fmt'` |
| 7 | **新的 `role === '…'`** | `grep -rn "role === '" src` | 多出一處 | 改走 `lib/roles.ts` |
| 9 | **新建的表前端讀不到（`permission denied for table …`）** | 建表的 migration 跑完，前端那頁 | 一次 | 10-30 起 Supabase 不自動 grant —— 補 `grant select, insert, update, delete on public.那張表 to authenticated, service_role;`。留底查詢：`一次性腳本/查-Data-API權限-哪些表沒給authenticated.sql` |
| 8 | **請假時數對不上（`HOURS_MISMATCH`）** | 有人請假送不出去、訊息含這個字 | 一次 | 前端 `lib/leave-days.ts` 跟資料庫 `leave_hours_between()` 兩份算式歪了 —— 兩邊一起看，不要只修一邊 |

## 已完成（留一行就好，細節看 README 8.2）

| 日期 | 項目 |
|---|---|
| 09-22 | 架構體檢；53 個沒用到的東西、伺服器端日期、`contracts` 拿掉 `setTimeout`、死檔搬走 |
| 09-23 | 五支 RPC；請假改版；展延留底；列數檢查 11 處；訊息／權限／金額格式各收一份；備品與稅務表格可橫向捲 |
| 09-23 | 月租單每期應收對回契約金額（298）；6 張年繳已收第 1 期補回餘數；收租「與契約不符」改比每期金額 |
| 09-24 | Supabase 10-30 不再自動 grant：規矩進 CLAUDE.md、留底查詢；現況 101 張全部 ✅ |

# 帳戶管理（規格）

> 狀態：**做完了，等實機測 PDF**（2026-08-18）
> 三個銀行帳戶的流水鏡像。會計上傳對帳單 PDF，系統解析、去重、更新餘額。

---

## 為什麼要做

現在三個帳戶的餘額散在網銀裡。要知道「公司現在有多少現金」得逐一登入查。
而對帳時要翻 PDF 逐筆比對，那份 PDF 看完就關掉，下次再翻一次。

**把流水存進 ERP，是為了讓「現在有多少錢」變成一個看得到的數字。**

---

## 一、範圍

| | |
|---|---|
| **這一版做** | 流水鏡像 —— 存交易、顯示餘額、可搜尋 |
| **這一版不做** | 跟 ERP 的訂單／收款／支出對帳 |
| 權限 | 會計 / 主管 / 總經理（`accountant` / `manager` / `super_admin`） |

### 三個帳戶（三份 PDF 實測確認 2026-08-18）

| 末五碼 | 完整帳號 | 期初 | 2025/06/30 餘額 | 筆數 |
|---|---|---:|---:|---:|
| **70564** | 20992000170564 | 28,107 | **81,977** | 47 |
| **24145** | 2176200002**4145** | 0 | **6,590** | 7 |
| **48088** | 20992000148088 | 4,504 | **262,433** | 198 |
| | | | **351,000** | 252 |

另外還有一份 **2026/07/01–08/18 的 24145**（132 筆，期末 3,293,461）——
它是第四個測試素材，帶來前三份沒有的兩件事：主列備註、以及銀行印錯的餘額。

三個都是**元大中崙・綜合活期** —— 一支解析器就夠。

**但 24145 的號碼是另一個系列**（`21762` 開頭，其餘兩個是 `20992`）。
同一家分行、同一種帳戶，前綴卻不同 ——
「三個帳號長得像，比前面幾碼就好」這種捷徑在這裡就會出錯。

24145 是 **2025/06 才開的戶**（期初 0，只有 7 筆）。

---

## 二、版面

```
┌─ 帳戶管理 ──────────────────────────────────────────┐
│  ┌────────────┐ ┌────────────┐ ┌────────────┐     │
│  │ 元大 70564 │ │ 元大 24145 │ │ 元大 48088 │     │
│  │   $81,977  │ │    $6,590  │ │  $262,433  │     │
│  │  至 06/30  │ │  至 06/30  │ │  至 06/30  │     │ ← 餘額 ＋ 對帳單截止日
│  └────────────┘ └────────────┘ └────────────┘     │
│                                    [⬆ 上傳對帳單]   │
├────────────────────────────────────────────────────┤
│  70564 │ 24145 │ 48088                              │
│  日期    摘要        對方       支出    存入    餘額  │
│  06/24  ＡＴＭ轉    國泰世華          80,000  81,977 │
│  06/23  企網付款    網內互轉  45,000          1,977 │
└────────────────────────────────────────────────────┘
```

**卡片要寫「至 MM/DD」** —— 只給一個數字的話，看的人不知道那是今天的還是三個月前的。
而餘額是「最後一次上傳的對帳單的期末」，不是即時的。

**最上面要有一個合計**（使用者確認 2026-08-18）：三個戶加起來 **351,000**（2025/06/30）。

合計有個陷阱：三份對帳單的截止日可能不一樣。
只上傳了兩份就顯示合計的話，那個數字是「兩個新的 ＋ 一個舊的」——
看起來像現在的現金，其實不是。

**所以合計旁邊要標最舊那一份的日期**，例如
「總計 $351,000（其中 24145 只到 03/31）」。
三份都對齊時就只寫一個日期。

---

## 三、上傳流程：丟檔案進去就好

> 使用者指定（2026-08-18）：
> 「不用選帳號，直接讀 PDF，裡面也有帳號。」

```
① 丟檔案（可一次多份）
      │
      ▼
② 從 PDF 認帳號
      │   帳號元大中崙-綜合活期-20992000170564
      │        │
      │        ├─ 認不出來    → 停：這份 PDF 找不到帳號
      │        └─ 系統沒登記  → 停：帳號 209920001705**64** 尚未建立，請先去建帳戶
      │
      ▼
③ 驗格式（**不寫入任何資料**）
      │  ├ 表頭還在不在、座標有沒有跑掉？
      │  ├ 逐筆餘額連不連得起來？
      │  └ 總計對不對得上？
      │
      ▼
④ 預覽：元大中崙 · 2025/01–06 · 新增 12 筆 / 重複 35 筆
      │
      ▼
⑤ 按「確認匯入」才寫進資料庫
```

### 為什麼不讓人選

PDF 裡就有帳號：

```
帳號元大中崙-綜合活期-20992000170564
```

**已經知道答案的事情不要問。** 讓人選再驗，是多開一個出錯的地方 ——
選錯了系統會擋（所以不危險），但那個擋是白費的一次來回。
直接讀，連選錯的機會都不存在。

順帶多一件事變得可行：**一次拖三份 PDF 進來**，各自認各自的帳戶。
要人選的話這件事做不到，因為三份要選三次。

### 認不出來的時候要停，不要猜

| 情況 | 做什麼 |
|---|---|
| PDF 裡抓不到帳號 | 停。「這份 PDF 找不到帳號 —— 是對帳單嗎？」 |
| 抓到了但系統沒這個帳戶 | 停。**把抓到的帳號印出來**，請人先去建帳戶 |
| 抓到兩個不同帳號 | 停。多半是兩份 PDF 被合併成一份了 |

第二項特別重要：**把帳號印出來**。
只說「查無此帳戶」的話，人不知道是自己傳錯檔還是帳戶沒建 ——
而把數字放上去，兩秒就分得出來。

### 比對規則

比對前把非數字都去掉（`2099-2000-170564` 與 `20992000170564` 是同一個）。

三個完整帳號都拿到了，所以**一律比完整帳號**。
`account_no_tail` 是退路 —— 將來多開一個帳戶而還沒拿到 PDF 時才用得上。

**不論比完整帳號還是末五碼，都只准比「解析出來的帳號欄位」，
不可以在整份 PDF 的文字裡搜。**
元大這份的票據號碼長這樣：

```
012-0000341168247682
```

那串數字裡隨時會出現 `24145` 或 `48088` —— 全文搜會撞上票據號碼，
然後整份流水記到錯的帳戶，靜靜地。

### 格式檢查具體檢查什麼

| 檢查 | 不過的意思 | 擋不擋 |
|---|---|---|
| 抽得到文字 | 這是掃描的 PDF，不是網銀下載的 | **擋** |
| 認得出帳號、且系統有登記 | 見上表 | **擋** |
| 第 1 頁找得到「支出金額 / 存入金額 / 帳面餘額」表頭 | 銀行改版，欄位換名字了 | **擋** |
| 三個表頭由左而右的順序正確 | 欄位順序變了 | **擋** |
| 解析出的筆數 > 0 | 期間內沒有交易，或解析失敗 | **擋** |
| PDF 上的序號 1…N 連續 | **有列被漏掉了** | **擋** |
| `sum(支出)` `sum(存入)` 等於 footer 的「總計」 | 有筆漏了 | **擋** |
| 逐筆「前一筆餘額 ± 金額 = 這筆餘額」 | 有筆漏了，或支出／存入判反了 | **看情況** —— 見下方「銀行自己把一格餘額印錯」 |

### ⚠️ 不可以檢查 x 座標落在固定範圍

原本規格寫「三個金額欄的 x 座標在預期範圍（±15pt）就放行，否則擋下」。
**實測發現這條規則會把 24145 整份擋掉。**

| | 70564 | 48088 | **24145** |
|---|---:|---:|---:|
| 支出金額 x0 | 240.0 | 240.0 | **301.9** |
| 存入金額 x0 | 286.4 | 286.4 | **358.4** |
| 帳面餘額 x0 | 330.8 | 330.8 | **417.6** |

同一家銀行、同一個系統匯出、同一個查詢期間 —— 整張表右移了 62pt。
（欄寬會跟著內容自動撐開，24145 的交易說明比較長。）

**所以座標不能寫死。每一份 PDF 自己讀表頭，用讀到的位置切欄。**
要驗的是**相對關係**（支出在存入左邊、存入在餘額左邊），不是絕對位置。

### 為什麼大部分都是「擋」

前六項沒過就代表解析器真的錯了，不是資料本身有瑕疵 ——
放行只會讓錯的流水進資料庫，而餘額歪掉是「慢慢地、不報錯地」發生。

**只有「餘額連續」是例外**，因為它可能是銀行印錯而不是我們讀錯 ——
分辨的方法是看總計與期初期末對不對得起來。詳見下方。

---

## 四、解析：三個會安靜漏資料的坑

### 坑① 純文字看不出支出還是存入

抽出來的文字長這樣：

```
1  2025/01/02 ＡＴＭ轉 台北富邦  17,836  45,943
11 2025/02/10 企網付款          217,575 44,334
```

**看不出 17,836 是存入、217,575 是支出** —— 兩欄之間的空白在純文字裡消失了。

x 座標分得出來（70564）：`17,836` 右緣 x1=325.4 落在存入欄，
`217,575` 右緣 x1=279.0 落在支出欄，餘額一律 x1=367.1。

金額**靠右對齊**，所以用「右緣 x1」比「左緣 x0」穩 ——
位數不同的數字左緣會飄，右緣不會。

**判錯一筆的後果**：那一筆之後的餘額全部偏掉兩倍金額，
而且**不會報錯** —— 只會讓卡片上的數字慢慢跟銀行對不上。

### 坑② 表頭只印在第 1 頁

48088 有 6 頁：

```
p1  表頭=['支出金額','存入金額','帳面餘額']   ← 只有這一頁有
p2  表頭=[]
p3  表頭=[]
p4  表頭=[]   ← 資料照樣印，沒有表頭
p5  表頭=[]
p6  表頭=[]
```

「找不到表頭就跳過這一頁」是最自然的寫法 ——
**而那會讓 198 筆只讀到 32 筆，另外 166 筆靜靜消失。**
金額欄位判斷全部正確，總計卻對不上，然後查半天。

**第 1 頁讀到的欄位位置要記下來，後面每一頁沿用。**

### 坑③ 票據號碼在餘額欄的右邊

```
2025/06/05 7176235030200100     ← 這是票據號碼，x1=547
1 2025/06/05 票據轉帳 1,000,000 1,000,000
13:07:00 0374507                ← 這也是，x1=529
```

只寫「最右邊的數字是餘額」的話，`7176235030200100` 會被當成餘額。

**要有右界**：x1 超過「備註票據號碼」表頭的左緣，就不是金額欄。

### 一筆交易佔三行

```
2025/01/01 012-0000341168247682        ← 交易日 ＋ 票據號碼
1 2025/01/02 ＡＴＭ轉 台北富邦 17,836 45,943   ← 序號 帳務日 說明 行庫 金額 餘額
01:06:44 １２月房租                     ← 時間 ＋ 摘要
```

依 `top` 座標分組，容差 3pt。**中間那行有序號**，靠它認出哪一行是主列。

### 交易日 ≠ 帳務日

`2025/01/01` 交易、`2025/01/02` 入帳 —— **兩個都要存**。
餘額的順序跟著**帳務日**走，對帳時人看的是**交易日**。
只存一個的話，另一種需求出現時就沒有資料了。

### 序號是免費的第三道驗證

PDF 上的序號是 1…N 連續的（三份都驗過）。
**不能拿它當去重鑰匙**（換查詢期間就從 1 重來），
但拿來檢查「有沒有漏讀」剛剛好 —— 坑②那種整頁消失，序號一斷就抓到。

### 期初餘額不用人填

第一筆的「餘額 − 存入 + 支出」就是期初。三份實測：

```
70564   45,943 −    17,836 =  28,107
24145  1,000,000 − 1,000,000 =      0   ← 2025/06 才開戶
48088   84,473 −    79,969 =   4,504
```

人填會填錯，而填錯了整條餘額鏈都對不上，
然後每次匯入都跳警告，最後沒有人再看警告。

---

## 五、去重

### 鑰匙

```
account_id ＋ 帳務日 ＋ 帳面餘額 ＋ 交易時間
```

**為什麼不用序號**：PDF 上的 `1, 2, 3…` 是**本次查詢的流水號**。
換一個查詢期間就從 1 重來 —— 拿它當鑰匙，第二次上傳會把第一筆蓋掉第一筆。

**為什麼餘額比金額可靠**：同一天可能收兩筆一樣的房租（都是 46,000），金額會撞。
但餘額不會 —— 第一筆之後餘額變了，第二筆的餘額必然不同。

**為什麼還要交易時間**：唯一撞得到的情況是「同一天先進 X 元再出 X 元」，
餘額回到原點。那時靠時間分。

### 重複要看得見，不能靜靜跳過

匯入完要說：

```
這次 47 筆 —— 新增 12 筆、重複 35 筆
                     [看是哪 35 筆]
```

跳過的東西沒有痕跡的話，數字對不上時查不出是「漏了」還是「本來就沒有」。

---

## 六、資料結構（草案）

```
bank_accounts  帳戶主檔
├ id, name              「元大 70564」
├ bank                  「元大銀行」
├ account_no            20992000170564  ← 上傳時拿 PDF 認到的帳號比對這一欄
├ account_no_tail       末五碼。退路,現在用不到
├ parser                'yuanta'  哪一支解析器（將來換銀行才會有第二支）
├ opening_balance       期初餘額（從第一份對帳單推導,不是人填）
├ sort, active
└ ※ 沒有 balance 欄 —— 餘額讀最新那份對帳單的 closing_balance

bank_statements  上傳批次
├ id, account_id
├ period_from, period_to    PDF 上的查詢期間
├ closing_balance           期末餘額  ← **卡片上的數字讀這裡**
├ total_debit, total_credit footer 的「總計」,驗證用
├ parsed_count              解析出幾筆
├ inserted_count            實際寫入幾筆
├ skipped_count             重複跳過幾筆
├ warnings                  驗不過但人確認後仍匯入的項目
├ file_name
├ uploaded_by, uploaded_at
└ note

bank_transactions  流水
├ id, account_id, statement_id
├ seq                   PDF 上的序號（同日排序 ＋ 檢查有沒有漏讀）
├ txn_date              交易日
├ post_date             帳務日   ← 排序與餘額連續性看這個
├ txn_time              交易時間
├ description           交易說明（ＡＴＭ轉／企網付款／現金存款／存款息…）
├ counterparty          交易行庫（台北富邦／中華郵政…）
├ debit, credit         支出／存入（其中一個是 0）
├ balance               帳面餘額  ← 去重鑰匙的一部分
├ memo                  摘要（１２月房租／南５／林思瑜３月租金…）
├ ref_no                票據號碼
└ created_at

唯一索引：(account_id, post_date, balance, coalesce(txn_time,'00:00:00'))
         ↑ 一定要 coalesce —— null 在唯一索引裡互不相等,
           沒印時間的兩筆會重複匯入
```

### 摘要是將來對帳的線索

```
１２月房租        ← 全形數字
南５             ← 房源代號
林思瑜３月租金     ← 房客名 ＋ 月份
開封店面押租       ← 物業 ＋ 用途
Ｒｅｎｔ ｕｔｉｌｉｔｉｅｓ
```

**全形字要保留原樣存**，不要轉半形 ——
轉了之後跟 PDF 對不起來，而人核對時看的是 PDF。
比對時再轉，那是查詢的事不是儲存的事。

---

## 七、待確認

- [ ] 上傳的 PDF 要不要留檔？（Supabase Storage 的權限跟資料表 RLS 是兩套）
- [ ] 卡片上要不要顯示「跟上次上傳差幾天」——放太久的餘額是誤導
- [ ] 帳戶名稱要不要改成看得懂的？現在叫「元大 70564」「元大 24145」「元大 48088」

### 解析器跑在瀏覽器（使用者選定 2026-08-18）

```
會計拖 PDF 進上傳區
      │
      ▼
瀏覽器用 pdfjs 解析 ← 不上傳檔案，PDF 沒離開他的電腦
      │
      ▼
當場顯示預覽：認到哪個帳戶、幾筆、期間、新增/重複
      │
      ▼
按確認 → 只送解析結果的 JSON 給後端
      │
      ▼
**後端重驗一次**：餘額鏈 ＋ 總計 ＋ 序號連續 → 才寫入
```

**為什麼是瀏覽器**：pdfjs 本來就是瀏覽器函式庫，在它的原生環境跑最不會出意外。
伺服器不用裝 Python，部署不變。而且預覽是即時的 —— 檔案根本不用上傳一趟。

**代價，講在前面**：解析是在前端算的，後端收到的是一包 JSON。
所以**後端一定要重驗**，不能因為「前端已經驗過」就直接寫入 ——
不然任何人打 API 都能塞流水進去。三道驗證是純算術，在後端跑一次很便宜。

還有一個小代價：pdfjs 的 `getTextContent()` 回傳「文字塊」不是「詞」。
如果它把 `1 2025/01/02 ＡＴＭ轉 台北富邦` 併成一塊，x 座標就抓不到單一數字。
**這是實作第一步要先確認的事**，用 70564 那份試一次就知道 ——
併成一塊的話還有 `textContent.items` 的 `chars` 層可以退，但寫法會不一樣。

---

### ⚠️ 踩到過的誤報：守衛把好的 PDF 擋掉

第一版在解析**之前**先問「pdfjs 有沒有把整列黏成一塊」，規則是
「有沒有任何一塊裡裝了兩個數字中間隔空白」。

結果 24145 那份被整份擋掉，訊息還寫「銀行換了產生對帳單的方式」。
真兇是抬頭這一行：

```
列印日期時間：2026/08/07 11:53:01
                   ↑ 07 空白 11
```

那一行本來就該是一塊，跟表格一點關係也沒有。

**教訓有兩層，兩層都比這個 bug 本身重要：**

1. **順序錯了。** 能不能解析出正確的數字，答案在 `validate()` ——
   不在文字長相上。應該先解析，跑不出來才問為什麼。
   守衛是**解釋**，不是關卡。

2. **測試證明的是別的東西。** 那三條 `looksCombined` 測試全綠，
   因為素材是 pdfplumber 抽的，它把日期與時間切成兩個詞。
   **測試驗的是 pdfplumber 的行為，不是 pdfjs 的。**
   拿 A 的資料去測 B 的假設，全綠也不代表什麼。

現在的做法：先解析 → 有結果就走 → 沒結果才用守衛解釋原因，
而且會把 pdfjs 實際讀到的前 12 塊印出來（`describeWords`）——
「解析不出來」五個字對查問題沒有幫助，
是抽不到文字、欄位換位置、還是整列黏住，三種處理方式完全不同。

---

### ⚠️ 第二個誤判：抬頭比對依賴了「詞怎麼切」

守衛修好之後，24145 **表格解析完全正確（7 筆、期間對）**，
只有一行紅字：「找不到帳號」。

原因是同一份程式碼裡兩種寫法：

```ts
// 帳號 —— 直接在「用空白接起來」的文字上比
/帳號(\S+?)-(\S+?)-(\d{6,})/.exec(text)

// 期間 —— 先把空白拿掉再比
/查詢期間(\d{4}\/\d{2}\/\d{2})~/.exec(text.replace(/\s+/g, ''))
```

pdfplumber 把「帳號元大中崙-綜合活期-21762000024145」當成**一個詞**，
pdfjs 切成好幾塊 —— 接起來變成「帳號 元大中崙-…」，第一條就對不上。
**期間那條剛好有拿掉空白所以活下來。碰巧對不叫對。**

現在抬頭一律用「拿掉所有空白」的版本比對。
表格不能這樣做 —— 那裡的空白就是欄位的分界。

### ⚠️ 順手抓到的沉默漏洞：讀不到總計等於不檢查

修的時候發現 `validate()` 是這樣寫的：

```ts
if (st.totalDebit != null && Math.abs(sd - st.totalDebit) > 0.005) { ... }
```

footer 讀不到的話，**最強的那道檢查會安靜地不執行，而畫面上一片綠**。
「檢查跳過了」跟「檢查通過了」長得一模一樣 ——
那是所有沉默錯誤裡最貴的一種。

已改成：讀不到總計本身就是一項 `no_total`。

### 為什麼這三個 bug 全都在同一層

| | 出過事 |
|---|---|
| 數字層（座標 → 支出／存入／餘額） | **零次** |
| 文字層（抬頭比對、守衛規則） | 三次 |

不是巧合。數字層是拿三份真實對帳單的**座標**測的，
文字層卻是拿 pdfplumber 的**切詞結果**測的 ——
而正式環境跑的是 pdfjs，切法不一樣。

**素材只有一種切法，就測不到「換一種切法會怎樣」。**

現在補了 `shatter()`：把每個詞再切碎、甚至切到每個字元一塊，
確認抬頭照樣讀得到。數字不切 —— 那不是真實會發生的事，
而且真被腰斬的話（「2,08」「5,031」）資訊已經沒了，該報錯不該猜。

---

### ⚠️ 備註欄一直在被丟掉（2026-08-18 使用者指出）

第一版的摘要只讀「下一行去掉時間」。而備註欄其實**橫跨三行**：

```
上一行  2026/07/01                                    012-0000341168247682  ← 票據號碼
主列    3  2026/07/01 匯入匯款  2,280,000  7,232,252  京饌企業有限公司 板信民權
下一行  15:59:46                                      １２月房租
```

**主列那一格整個掉了。** 而且不只新的這份 —— 三份舊的也是：

```
48088 第 1 筆   美商炒飯吧科技股份有限公司台灣分公司
24145 第 22 筆  7月A棟租金
24145 第 20 筆  電視
24145 第 21 筆  家具
```

這是**將來跟訂單對帳最值錢的欄位**，而沒有人會發現它掉了 ——
因為那一格本來就常常是空的。

兩個修法上的細節：

- **界線用「帳面餘額表頭的右緣」不是「備註表頭的左緣」。**
  「京饌企業有限公司」x0=409，而備註表頭 x0=435.1 —— 內容會往左伸出表頭。
- **票據號碼與摘要用長相分，不用行號分。** 票據號碼會折成兩行
  （`7176235030200100` ＋ `0374507`），靠行號分的話下半段會被當成摘要。
  純數字與連字號 → 票據號碼，其餘 → 摘要。

### ⚠️ 銀行自己把一格餘額印錯

2026/07 那份 24145 的第 22 筆：

```
21  匯出匯款    37,756   餘額 5,307,081
22  匯出匯款 1,329,688   餘額 3,976,587   ← 算出來是 3,977,393，差 806
23  匯出匯款 2,657,459   餘額 1,319,934   ← 又接回正確的鏈
```

而同一份的支出加總 15,311,998、存入加總 13,925,207
**跟 footer 的總計一字不差**，期初 4,680,252 ＋ 存入 − 支出
也剛好等於期末 3,293,461。

兩條獨立的檢查都過了 → 一筆都沒漏、金額全部讀對 → **是銀行印錯那一格**。

**所以驗證分成兩級：**

| | 條件 | 做法 |
|---|---|---|
| `block` | 帳號讀不到、序號斷、總計對不上、讀不到總計 | 整份不匯 |
| `warn` | 餘額某一格接不上，**但總計與期初期末都對得起來** | 顯示，人看過可以匯，記進 `warnings` |

整份擋掉在這裡是錯的：資料是完整的，擋掉只會讓會計沒有數字可用，
而下次拿同一份 PDF 還是一樣擋。

但**一定要留痕跡** —— 放行的警告寫進 `bank_statements.warnings`。
將來數字對不上時，那裡是唯一查得到「當初就知道有這件事」的地方。

---

## 八、實作進度

- [x] 上傳流程定案：**不選帳號，從 PDF 認**（2026-08-18）
- [x] **三份 PDF 全部實測解析成功** —— 演算法確定可行（見下）
- [x] `migration_142`：三張表 ＋ RLS ＋ 三個帳戶（含期初餘額）
- [x] 決定解析器跑在瀏覽器（使用者選定 2026-08-18）
- [x] `src/lib/bank-statement.ts` ＋ 測試（三份真實對帳單當測試素材）
- [x] `src/lib/bank-import.ts` ＋ 測試（去重、帳戶比對、合計）
- [x] `src/lib/pdf-words.ts` —— pdfjs 接頭，含「黏成一塊」的偵測
- [x] `POST /api/bank-statements/import` —— 後端用同一份解析器重驗
- [x] `/accounts` 頁：合計 ＋ 三張卡片 ＋ 分頁流水 ＋ 拖檔上傳
- [x] 選單加入口（會計 / 主管 / 總經理）
- [x] 實機拖 PDF —— 第一次被守衛誤擋，已修（見上）
- [x] **pdfjs 的文字粒度確認夠細** —— 24145 實機解析出正確的 7 筆
- [x] 修抬頭比對（帳號讀不到）＋ 補「讀不到總計要報」
- [x] 補回主列的備註欄（三份舊的也一直在掉）
- [x] 驗證分 block / warn —— 銀行印錯一格餘額不該擋掉整份
- [ ] 再拖一次，確認都進得去

### 解析器已經寫好，還缺一個接頭

`bank-statement.ts` 吃的是 `Word[]`（`page` / `x0` / `x1` / `top` / `text`），
不碰 PDF 檔案。所以：

- **測試不需要 pdfjs、也不需要 PDF 檔** —— 素材是三份真實對帳單抽出來的座標
- **後端可以用同一份程式碼重驗**前端送來的 JSON
- 剩下要寫的只有「pdfjs 的輸出 → `Word[]`」那一小段轉接

那一小段是唯一還沒驗過的東西：pdfjs 的 `getTextContent()` 回傳「文字塊」，
如果它把 `1 2025/01/02 ＡＴＭ轉` 併成一塊，就得改用字元層級的位置。
**用 70564 那份試一次就知道。**

### 實測結果（2026-08-18）

| 帳號 | 筆數 | 支出總計 | 存入總計 | 序號連續 | 餘額連續 | 期末 |
|---|---:|---:|---:|:-:|:-:|---:|
| 70564 | 47 | 2,085,031 ✅ | 2,138,901 ✅ | ✅ | ✅ | 81,977 |
| 24145 | 7 | 993,571 ✅ | 1,000,161 ✅ | ✅ | ✅ | 6,590 |
| 48088 | 198 | 4,144,295 ✅ | 4,402,224 ✅ | ✅ | ✅ | 262,433 |

支出／存入總計與 PDF footer 的「總計」逐字相符，
序號 1…N 無斷點，逐筆餘額全部連得起來。

### 解析器一定要有的測試

支出／存入判反**不會報錯**，只會讓餘額慢慢跟銀行對不上。
所以測試要釘住的不是「解析得出東西」，而是：

**用三份真實 PDF 當測試資料**，每一份都釘住上表那六個數字。
另外針對三個坑各釘一條：

- `17,836` 在存入欄的 x 位置 → `credit`，`debit` 是 0；`217,575` → `debit`
- **48088 要讀滿 198 筆**（坑②：表頭只在第 1 頁，跳過無表頭的頁只會讀到 32 筆）
- **24145 的表頭 x 座標跟另外兩份差 62pt 也要能解析**（坑①的變體：座標不能寫死）
- `7176235030200100` 不可以被當成餘額（坑③：票據號碼在右邊）
- 24145 的期初推導出 0，不是 null 也不是 1,000,000

# 資料庫欄位清單

> 2026-09-22 從**線上資料庫**匯出。原始檔 `supabase/schema-live.txt`，
> 重跑用 `supabase/匯出-schema給ER圖.sql`。
> **ER 圖在 README 的〈附錄 A〉**，這一份是查欄位用的。

共 **100 張表、1,299 個欄位、158 條外鍵、187 個索引**（不含主鍵索引）。

★ 「大約幾列」是 `pg_class.reltuples` 的**估計值**，`0` 只代表沒跑過 `ANALYZE`，不代表空表。

## 目錄

* **主檔** — `estates`、`properties`、`property_listings`、`staff`、`profiles`、`staff_properties`、`estate_managers`、`estate_fee_default`、`customers`
* **收入 ① 契約與訂單** — `contracts`、`contract_payments`、`contract_recurring_charges`、`contract_order_conflicts`、`recurring_charges`、`orders`、`period_lock`、`period_unlock`、`order_lock_pending`
* **收入 ② 收款、押金與認列** — `order_payments`、`deposits`、`deposit_payments`、`revenue_recognitions`、`revenue_snapshots`、`invoices`
* **支出 ① 請款到支出** — `purchase_demands`、`purchase_demand_items`、`purchase_requests`、`purchase_request_items`、`expenses`、`other_book_payments`
* **憑證附件** — `attachments`
* **支出 ② 暫付與代碼主檔** — `advance_payments`、`advance_repayments`、`advance_repayment_lines`、`account_codes`、`payment_accounts`、`payee_presets`
* **銀行與稅** — `bank_accounts`、`bank_statements`、`bank_transactions`、`tax_invoice`、`tax_period`、`accounting_reports`
* **房務 ① 排班與工單** — `hk_event`、`hk_work_item`、`hk_work_split`、`hk_task`、`hk_day`、`hk_staff`、`hk_property`
* **房務 ② 設定、人事費與評價** — `hk_period`、`hk_month_property`、`hk_setting`、`hk_work_type`、`hk_labor_cost`、`hk_audit`、`cleaning_records`、`reviews`
* **備品** — `supply_item`、`supply_txn`、`supply_count`
* **人事與出勤** — `attendance`、`attendance_fixes`、`leave_types`、`leave_balances`、`leave_requests`、`leave_seniority`、`overtime_requests`、`work_settings`、`holidays`
* **系統 ① 通知、稽核與回收桶** — `notifications`、`notification_prefs`、`push_subscriptions`、`data_audit`、`trash`、`schema_migrations`、`app_secrets`
* **系統 ② 公告與董事會** — `announcements`、`announcement_reads`、`board_events`、`board_files`、`board_forms`、`board_secrets`、`board_secret_reads`
* **外部資料與行銷** — `airbnb_snapshots`、`sync_issues`、`sync_issue_log`、`sync_runs`、`sync_state`、`social_accounts`、`social_posts`、`social_splits`、`tender_feed`、`tenders`
* **備份／一次性** — `bank_statements_backup_144`、`bank_statements_last_wipe`、`deleted_contract_orders_81`、`deleted_stub_orders_93`、`deposits_fix_b6_b5`、`orders_backup_136`

---

---

# 主檔

誰、哪裡 —— 其他每一張表都指回這裡

## `estates`

物業主檔（安幸辦公室也是一筆）

主鍵 `id`　・　9 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `name` | text |  |  |  |
| `manager` | text | ✓ |  |  |
| `sort` | integer | ✓ | `99` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `active` | boolean |  | `true` |  |
| `gps_lat` | numeric | ✓ |  |  |
| `gps_lng` | numeric | ✓ |  |  |
| `gps_radius_m` | integer |  | `500` |  |

索引：

* **UNIQUE** `estates_name_key` — `btree (name)`

⚠ **刪一筆會被 12 條外鍵擋住**：`attendance.in_estate_id`、`attendance.out_estate_id`、`contracts.estate_id`、`deposits.estate_id`、`expenses.estate_id`、`orders.estate_id`、`properties.estate_id`、`purchase_demand_items.estate_id`、`purchase_request_items.estate_id`、`recurring_charges.estate_id`、`supply_item.estate_id`、`tax_invoice.estate_id`


## `properties`

房源主檔。`parent_property_id` 指向上層房源（整層拆成小房間時用），`units` 是這一筆算幾間、`count_in_occupancy` 決定要不要進住房率分母

主鍵 `id`　・　17 欄　・　大約 180 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `name` | text |  |  |  |
| `airbnb_listing_id` | text | ✓ |  |  |
| `name_aliases` | text[] | ✓ | `'{}'::text[]` |  |
| `active` | boolean |  | `true` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `parent_property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `beds` | integer | ✓ |  |  |
| `linen_group` | text |  | `'other'::text` |  |
| `ptype` | text |  | `'room'::text` |  |
| `count_linen` | boolean |  | `true` |  |
| `clean_points` | numeric(4,1) | ✓ |  |  |
| `clean_price` | numeric(12,2) | ✓ |  |  |
| `show_in_room_calendar` | boolean |  | `true` |  |
| `units` | integer |  | `1` |  |
| `count_in_occupancy` | boolean |  | `true` |  |

索引：

* `idx_properties_parent` — `btree (parent_property_id) WHERE (parent_property_id IS NOT NULL)`
* **UNIQUE** `properties_airbnb_listing_id_key` — `btree (airbnb_listing_id)`

⚠ **刪一筆會被 11 條外鍵擋住**：`cleaning_records.property_id`、`deposits.property_id`、`expenses.property_id`、`hk_property.property_id`、`hk_work_split.property_id`、`orders.property_id`、`properties.parent_property_id`、`purchase_request_items.property_id`、`recurring_charges.property_id`、`reviews.property_id`、`tax_invoice.property_id`


## `property_listings`

房源 ↔ Airbnb listing 的對應（一間房換過幾次 listing 都留著，`is_current` 標現在那一個）

主鍵 `listing_id`　・　5 欄　・　大約 76 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`listing_id`** 🔑 | text |  |  |  |
| `property_id` | uuid |  |  | → `properties.id` (CASCADE) |
| `is_current` | boolean |  | `false` |  |
| `note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* `property_listings_prop_idx` — `btree (property_id)`


## `staff`

員工主檔。`staff_type` 是**職位**（管家／房務／經理…），`role` 是**權限** —— 同一張表放了兩件事

主鍵 `id`　・　10 欄　・　大約 11 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `name` | text |  |  |  |
| `aliases` | text[] | ✓ | `'{}'::text[]` |  |
| `staff_type` | text |  | `'housekeeper'::text` |  |
| `active` | boolean |  | `true` |  |
| `sort` | integer | ✓ | `99` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `email` | text | ✓ |  |  |
| `role` | text | ✓ | `'housekeeper'::text` |  |
| `auth_uid` | uuid | ✓ |  |  |

索引：

* **UNIQUE** `staff_name_key` — `btree (name)`

⚠ **刪一筆會被 3 條外鍵擋住**：`cleaning_records.staff_id`、`estate_managers.staff_id`、`hk_staff.staff_id`


## `profiles`

登入帳號的側寫。`id` 就是 `auth.users.id`，`role` 是**權限**

主鍵 `id`　・　9 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  |  | → `users.id` (NO ACTION) |
| `name` | text |  |  |  |
| `role` | text |  | `'housekeeper'::text` |  |
| `active` | boolean |  | `true` |  |
| `work_start` | time without time zone | ✓ |  |  |
| `work_end` | time without time zone | ✓ |  |  |
| `work_hours_per_day` | numeric | ✓ |  |  |
| `hired_on` | date | ✓ |  |  |
| `is_social_editor` | boolean |  | `false` |  |

⚠ **刪一筆會被 21 條外鍵擋住**：`announcements.created_by`、`attachments.uploaded_by`、`attendance.edited_by`、`attendance_fixes.reviewed_by`、`deposit_payments.created_by`、`deposit_payments.deleted_by`、`deposits.admin_approved_by`、`deposits.manager_approved_by`、`deposits.refund_requested_by`、`deposits.rejected_by`、`deposits.transferred_by`、`hk_event.dismissed_by`、`leave_requests.admin_by`、`leave_requests.manager_by`、`overtime_requests.manager_by`、`purchase_demands.requester_id`、`sync_issue_log.acted_by`、`sync_issues.dismissed_by`、`trash.deleted_by`、`trash.purged_by`、`trash.restored_by`


## `staff_properties`

員工負責哪些房源

主鍵 `staff_id, property_id`　・　2 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`staff_id`** 🔑 | uuid |  |  | → `staff.id` (CASCADE) |
| **`property_id`** 🔑 | uuid |  |  | → `properties.id` (CASCADE) |


## `estate_managers`

物業負責人的**任期**（有起訖日，所以查得到「那一天是誰負責」）

主鍵 `id`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `estate_id` | uuid |  |  | → `estates.id` (CASCADE) |
| `staff_id` | uuid |  |  | → `staff.id` (NO ACTION) |
| `start_date` | date |  |  |  |
| `end_date` | date | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ |  |  |

索引：

* `estate_managers_no_overlap` — `gist (estate_id, daterange(start_date, COALESCE(end_date, 'infinity'::date), '[]'::text))`
* `idx_estate_managers_estate` — `btree (estate_id, start_date)`
* `idx_estate_managers_staff` — `btree (staff_id)`


## `estate_fee_default`

物業層級的預設費用（可不可以帶寵物、寵物押金與費用）

主鍵 `estate_id`　・　6 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`estate_id`** 🔑 | uuid |  |  | → `estates.id` (CASCADE) |
| `pet_allowed` | boolean |  | `true` |  |
| `pet_deposit` | numeric(14,2) | ✓ |  |  |
| `pet_fee` | numeric(14,2) | ✓ |  |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |
| `updated_by` | uuid | ✓ |  |  |


## `customers`

住客名冊。從訂單彙整出來的，`customers_key_uniq` 用「物業＋房源＋姓名」去重

主鍵 `id`　・　20 欄　・　大約 2,100 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (SET NULL) |
| `property_id` | uuid | ✓ |  | → `properties.id` (SET NULL) |
| `property_label` | text | ✓ |  |  |
| `name` | text |  |  |  |
| `stay_from` | date | ✓ |  |  |
| `stay_to` | date | ✓ |  |  |
| `stay_count` | integer |  | `0` |  |
| `src_phone` | text | ✓ |  |  |
| `src_kind` | text | ✓ |  |  |
| `phone` | text | ✓ |  |  |
| `email` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `estate_key` | text | ✓ | `COALESCE((estate_id)::text, '-'::text)` |  |
| `prop_key` | text | ✓ | `COALESCE((property_id)::text, NULLIF(btrim(COALESCE(property_label, ''::text)), ''::text), '-'::text)` |  |
| `name_key` | text | ✓ | `lower(regexp_replace(COALESCE(name, ''::text), '\\s+'::text, ''::text, 'g'::text))` |  |
| `stale` | boolean |  | `false` |  |
| `synced_at` | timestamp with time zone | ✓ |  |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* `cust_estate_idx` — `btree (estate_id, name)`
* `cust_stay_idx` — `btree (stay_to DESC)`
* **UNIQUE** `customers_key_uniq` — `btree (estate_key, prop_key, name_key)`


---

# 收入 ① 契約與訂單

長租契約自動長出月租單，短租訂單從 Airbnb 同步進來

## `contracts`

長租契約。月租單由它自動長出來

主鍵 `id`　・　40 欄　・　大約 110 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `name` | text |  |  |  |
| `type` | text |  | `'longterm'::text` |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `property_raw` | text | ✓ |  |  |
| `tenant_name` | text | ✓ |  |  |
| `start_date` | date | ✓ |  |  |
| `end_date` | date | ✓ |  |  |
| `amount_per_period` | numeric | ✓ |  |  |
| `cadence` | text |  | `'monthly'::text` |  |
| `account` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `active` | boolean |  | `true` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `room` | text | ✓ |  |  |
| `monthly_rent` | numeric | ✓ |  |  |
| `deposit` | numeric | ✓ |  |  |
| `phone` | text | ✓ |  |  |
| `paid` | boolean | ✓ | `false` |  |
| `pay_day` | integer | ✓ |  |  |
| `deposit_received` | boolean | ✓ | `false` |  |
| `deposit_received_at` | date | ✓ |  |  |
| `deposit_returned` | boolean | ✓ | `false` |  |
| `deposit_returned_at` | date | ✓ |  |  |
| `first_payment_date` | date | ✓ |  |  |
| `auto_renew` | boolean |  | `false` |  |
| `watch` | boolean |  | `false` |  |
| `display_name` | text | ✓ |  |  |
| `invoice_required` | boolean |  | `false` |  |
| `invoice_day` | smallint | ✓ |  |  |
| `invoice_after_paid` | boolean |  | `true` |  |
| `invoice_title` | text | ✓ |  |  |
| `invoice_tax_id` | text | ✓ |  |  |
| `invoice_note` | text | ✓ |  |  |
| `concessions` | jsonb |  | `'[]'::jsonb` |  |
| `fx_deposit` | jsonb |  | `'[]'::jsonb` |  |
| `earnest_amount` | numeric |  | `0` |  |
| `earnest_only` | boolean |  | `false` |  |
| `tax_free` | boolean |  | `false` |  |
| `purpose_type` | text |  | `'estate'::text` |  |


## `contract_payments`

契約的分期收款（每一期起訖、金額、收了沒）

主鍵 `id`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `contract_id` | uuid | ✓ |  | → `contracts.id` (CASCADE) |
| `period_start` | date |  |  |  |
| `period_end` | date |  |  |  |
| `amount` | numeric |  |  |  |
| `confirmed` | boolean |  | `false` |  |
| `confirmed_at` | timestamp with time zone | ✓ |  |  |
| `order_id` | uuid | ✓ |  | → `orders.id` (NO ACTION) |

索引：

* **UNIQUE** `contract_payments_contract_id_period_start_key` — `btree (contract_id, period_start)`


## `contract_recurring_charges`

契約的固定加收（管理費、車位…），每個月跟著月租單一起產生

主鍵 `id`　・　10 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `contract_id` | uuid |  |  | → `contracts.id` (CASCADE) |
| `fee_type` | text |  |  |  |
| `item_name` | text | ✓ |  |  |
| `amount` | numeric |  | `0` |  |
| `start_ym` | text |  |  |  |
| `end_ym` | text | ✓ |  |  |
| `active` | boolean |  | `true` |  |
| `note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* `crc_contract_idx` — `btree (contract_id)`


## `contract_order_conflicts`

月租單該產生卻產生不了的紀錄（撞到已存在的單），給畫面提示用

主鍵 `contract_id, ym`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`contract_id`** 🔑 | uuid |  |  | → `contracts.id` (CASCADE) |
| **`ym`** 🔑 | text |  |  |  |
| `room` | text | ✓ |  |  |
| `want_start` | date | ✓ |  |  |
| `want_end` | date | ✓ |  |  |
| `want_amount` | numeric | ✓ |  |  |
| `blocked_by_key` | text | ✓ |  |  |
| `seen_at` | timestamp with time zone |  | `now()` |  |


## `recurring_charges`

物業／房源層級的定期收費（跟契約無關的那種）

主鍵 `id`　・　12 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `estate_id` | uuid |  |  | → `estates.id` (NO ACTION) |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `property_raw` | text | ✓ |  |  |
| `fee_type` | text |  |  |  |
| `item_name` | text |  |  |  |
| `amount` | numeric |  | `0` |  |
| `start_ym` | text |  |  |  |
| `end_ym` | text | ✓ |  |  |
| `active` | boolean |  | `true` |  |
| `note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* **UNIQUE** `uq_recurring_charge` — `btree (estate_id, COALESCE(property_id, '00000000-0000-0000-0000-000000000000'::uuid), fee_type, item_name)`


## `orders`

**訂單** —— 短租、月租單、一次性費用、加費全部在這一張。`order_key` 是冪等鍵，`imported_via` 說它從哪來

主鍵 `id`　・　41 欄　・　大約 5,310 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `order_key` | text |  |  |  |
| `source` | text |  |  |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `property_raw` | text | ✓ |  |  |
| `guest_name` | text | ✓ |  |  |
| `checkin` | date | ✓ |  |  |
| `checkout` | date | ✓ |  |  |
| `nights` | integer |  | `0` |  |
| `amount` | numeric |  | `0` |  |
| `deposit` | numeric | ✓ | `0` |  |
| `account` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `contract_id` | uuid | ✓ |  | → `contracts.id` (CASCADE) |
| `imported_via` | text | ✓ | `'manual'::text` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `paid` | boolean |  | `false` |  |
| `deposit_received` | boolean | ✓ | `false` |  |
| `deposit_returned` | boolean | ✓ | `false` |  |
| `parent_order_id` | uuid | ✓ |  |  |
| `fee_type` | text | ✓ |  |  |
| `fx_revenue` | jsonb |  | `'[]'::jsonb` |  |
| `fx_deposit` | jsonb |  | `'[]'::jsonb` |  |
| `move_group` | uuid | ✓ |  |  |
| `deposit_received_at` | date | ✓ |  |  |
| `deposit_returned_at` | date | ✓ |  |  |
| `paid_at` | date | ✓ |  |  |
| `item_name` | text | ✓ |  |  |
| `paid_amount` | numeric |  | `0` |  |
| `invoice_required` | boolean |  | `false` |  |
| `invoice_title` | text | ✓ |  |  |
| `invoice_tax_id` | text | ✓ |  |  |
| `account_code` | text | ✓ |  | → `account_codes.code` (NO ACTION) |
| `deposit_id` | uuid | ✓ |  | → `deposits.id` (SET NULL) |
| `book` | text |  | `'anxing'::text` |  |
| `pet_deposit` | numeric(14,2) | ✓ |  |  |
| `purpose_type` | text |  | `'estate'::text` |  |
| `move_chain` | text | ✓ |  |  |
| `earnest_amount` | numeric |  | `0` |  |
| `earnest_only` | boolean |  | `false` |  |

索引：

* `idx_orders_checkin` — `btree (checkin)`
* `idx_orders_checkout` — `btree (checkout)`
* `idx_orders_contract_id` — `btree (contract_id) WHERE (contract_id IS NOT NULL)`
* `idx_orders_estate` — `btree (estate_id)`
* `idx_orders_item_name` — `btree (item_name) WHERE (item_name IS NOT NULL)`
* `idx_orders_source` — `btree (source)`
* `orders_account_code_idx` — `btree (account_code)`
* `orders_book_idx` — `btree (book)`
* `orders_deposit_id_idx` — `btree (deposit_id) WHERE (deposit_id IS NOT NULL)`
* **UNIQUE** `orders_order_key_key` — `btree (order_key)`
* **UNIQUE** `uq_contract_order_month` — `btree (contract_id, checkin) WHERE ((imported_via = 'contract'::text) AND (contract_id IS NOT NULL))`
* **UNIQUE** `uq_orders_order_key` — `btree (order_key) WHERE (order_key IS NOT NULL)`

⚠ **刪一筆會被 1 條外鍵擋住**：`contract_payments.order_id`


## `period_lock`

月結關帳。關掉之後那個月的單不能改

主鍵 `ym`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`ym`** 🔑 | text |  |  |  |
| `locked` | boolean |  | `true` |  |
| `auto` | boolean |  | `false` |  |
| `locked_at` | timestamp with time zone | ✓ |  |  |
| `locked_by` | uuid | ✓ |  |  |
| `reopened_at` | timestamp with time zone | ✓ |  |  |
| `reopened_by` | uuid | ✓ |  |  |
| `note` | text | ✓ |  |  |


## `period_unlock`

臨時解鎖（30 分鐘後自動失效），每一次解鎖都留一筆

主鍵 `id`　・　8 欄　・　大約 61 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `user_id` | uuid | ✓ | `auth.uid()` |  |
| `ym` | text |  |  |  |
| `order_id` | uuid | ✓ |  | → `orders.id` (CASCADE) |
| `contract_id` | uuid | ✓ |  | → `contracts.id` (CASCADE) |
| `granted_at` | timestamp with time zone |  | `now()` |  |
| `expires_at` | timestamp with time zone |  | `(now() + '00:30:00'::interval)` |  |
| `note` | text | ✓ |  |  |

索引：

* `period_unlock_live_idx` — `btree (user_id, ym, expires_at)`


## `order_lock_pending`

關帳後被擋下來的修改，留著等人決定要不要補

主鍵 `id`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `order_id` | uuid |  |  | → `orders.id` (CASCADE) |
| `ym` | text |  |  |  |
| `changes` | jsonb |  |  |  |
| `attempted_at` | timestamp with time zone |  | `now()` |  |
| `resolved` | boolean |  | `false` |  |
| `resolved_at` | timestamp with time zone | ✓ |  |  |
| `resolved_by` | uuid | ✓ |  |  |

索引：

* `order_lock_pending_open_idx` — `btree (ym) WHERE (NOT resolved)`


---

# 收入 ② 收款、押金與認列

錢進來，以及「這筆算哪個月的營收」

## `order_payments`

訂單的收款紀錄（一張單可以收好幾次）

主鍵 `id`　・　11 欄　・　大約 199 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `order_id` | uuid |  |  | → `orders.id` (CASCADE) |
| `paid_on` | date |  |  |  |
| `amount` | numeric |  |  |  |
| `account` | text | ✓ |  | → `payment_accounts.code` (NO ACTION) |
| `note` | text | ✓ |  |  |
| `created_by` | uuid | ✓ |  | → `users.id` (NO ACTION) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `method` | text |  | `'transfer'::text` |  |
| `fee_amount` | numeric |  | `0` |  |
| `fee_on` | date | ✓ |  |  |

索引：

* `op_order_idx` — `btree (order_id)`
* `op_paid_on_idx` — `btree (paid_on DESC)`


## `deposits`

押金與訂金。`kind` 分兩種，`lines` 存多幣別明細，退款走三段式核准

主鍵 `id`　・　48 欄　・　大約 115 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `order_id` | uuid | ✓ |  | → `orders.id` (SET NULL) |
| `contract_id` | uuid | ✓ |  | → `contracts.id` (SET NULL) |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `room` | text | ✓ |  |  |
| `guest_name` | text | ✓ |  |  |
| `currency` | text |  | `'TWD'::text` |  |
| `amount` | numeric |  | `0` |  |
| `received_on` | date | ✓ |  |  |
| `received_method` | text | ✓ |  |  |
| `received_account` | text | ✓ |  |  |
| `returned_on` | date | ✓ |  |  |
| `returned_method` | text | ✓ |  |  |
| `returned_account` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `orphaned` | boolean |  | `false` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `is_manual` | boolean |  | `false` |  |
| `refund_status` | text |  | `'none'::text` |  |
| `payee_bank_code` | text | ✓ |  |  |
| `payee_name` | text | ✓ |  |  |
| `payee_account` | text | ✓ |  |  |
| `planned_refund_on` | date | ✓ |  |  |
| `refund_requested_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `refund_requested_at` | timestamp with time zone | ✓ |  |  |
| `manager_approved_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `manager_approved_at` | timestamp with time zone | ✓ |  |  |
| `admin_approved_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `admin_approved_at` | timestamp with time zone | ✓ |  |  |
| `rejected_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `rejected_at` | timestamp with time zone | ✓ |  |  |
| `reject_reason` | text | ✓ |  |  |
| `lines` | jsonb |  | `'[]'::jsonb` |  |
| `transfer_to_id` | uuid | ✓ |  | → `deposits.id` (SET NULL) |
| `transfer_from_id` | uuid | ✓ |  | → `deposits.id` (SET NULL) |
| `transferred_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `transferred_at` | timestamp with time zone | ✓ |  |  |
| `received_amount` | numeric |  | `0` |  |
| `refund_amount` | numeric | ✓ |  |  |
| `kind` | text |  | `'deposit'::text` |  |
| `converted_to_deposit_id` | uuid | ✓ |  | → `deposits.id` (NO ACTION) |
| `converted_from_earnest_id` | uuid | ✓ |  | → `deposits.id` (NO ACTION) |
| `converted_by` | uuid | ✓ |  |  |
| `converted_at` | timestamp with time zone | ✓ |  |  |
| `forfeited_on` | date | ✓ |  |  |
| `forfeit_order_id` | uuid | ✓ |  | → `orders.id` (SET NULL) |
| `forfeited_by` | uuid | ✓ |  |  |

索引：

* `dep_estate_idx` — `btree (estate_id)`
* `dep_received_idx` — `btree (received_on)`
* **UNIQUE** `deposits_contract_kind_uidx` — `btree (contract_id, currency, kind) WHERE (contract_id IS NOT NULL)`
* **UNIQUE** `deposits_order_kind_uidx` — `btree (order_id, kind) WHERE (order_id IS NOT NULL)`

⚠ **刪一筆會被 2 條外鍵擋住**：`deposits.converted_from_earnest_id`、`deposits.converted_to_deposit_id`


## `deposit_payments`

押金的收款紀錄

主鍵 `id`　・　11 欄　・　大約 95 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `deposit_id` | uuid |  |  | → `deposits.id` (CASCADE) |
| `paid_on` | date |  |  |  |
| `amount` | numeric |  |  |  |
| `method` | text | ✓ |  |  |
| `account` | text | ✓ |  | → `payment_accounts.code` (NO ACTION) |
| `note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `deleted_at` | timestamp with time zone | ✓ |  |  |
| `deleted_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |

索引：

* `dep_pay_alive_idx` — `btree (deposit_id) WHERE (deleted_at IS NULL)`
* `dep_pay_dep_idx` — `btree (deposit_id, paid_on)`


## `revenue_recognitions`

**營收認列** —— 一張跨月的單拆成每個月多少錢

主鍵 `id`　・　22 欄　・　大約 5,951 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `order_id` | uuid | ✓ |  | → `orders.id` (CASCADE) |
| `ym` | text |  |  |  |
| `period_start` | date |  |  |  |
| `period_end` | date |  |  |  |
| `source` | text |  |  |  |
| `estate_id` | uuid | ✓ |  |  |
| `property_id` | uuid | ✓ |  |  |
| `estate_name` | text | ✓ |  |  |
| `property_raw` | text | ✓ |  |  |
| `guest_name` | text | ✓ |  |  |
| `checkin` | date | ✓ |  |  |
| `checkout` | date | ✓ |  |  |
| `total_amount` | numeric | ✓ |  |  |
| `total_nights` | integer | ✓ |  |  |
| `month_nights` | integer | ✓ |  |  |
| `month_amount` | numeric |  |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `fee_type` | text | ✓ |  |  |
| `item_name` | text | ✓ |  |  |
| `contract_id` | uuid | ✓ |  | → `contracts.id` (CASCADE) |
| `purpose_type` | text | ✓ |  |  |

索引：

* `idx_recog_contract` — `btree (contract_id)`
* `idx_recog_order` — `btree (order_id)`
* `idx_recog_ym` — `btree (ym)`
* **UNIQUE** `uq_recognition_order_month` — `btree (order_id, ym) WHERE (order_id IS NOT NULL)`


## `revenue_snapshots`

早期匯入的營收快照（沒有訂單可以對應的歷史資料）

主鍵 `id`　・　14 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `ym` | text |  |  |  |
| `source` | text |  |  |  |
| `estate_name` | text | ✓ |  |  |
| `property_raw` | text | ✓ |  |  |
| `guest_name` | text | ✓ |  |  |
| `checkin` | date | ✓ |  |  |
| `checkout` | date | ✓ |  |  |
| `total_amount` | numeric | ✓ |  |  |
| `month_amount` | numeric |  |  |  |
| `month_nights` | integer | ✓ |  |  |
| `total_nights` | integer | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* `idx_snap_estate` — `btree (estate_name)`
* `idx_snap_ym` — `btree (ym)`


## `invoices`

開出去的發票（安幸自己開的，不是稅務那張）

主鍵 `id`　・　14 欄　・　大約 49 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `contract_id` | uuid | ✓ |  | → `contracts.id` (SET NULL) |
| `order_id` | uuid | ✓ |  | → `orders.id` (SET NULL) |
| `room` | text |  |  |  |
| `ym` | text |  |  |  |
| `amount` | numeric | ✓ |  |  |
| `invoice_no` | text |  |  |  |
| `invoice_date` | date |  |  |  |
| `title` | text | ✓ |  |  |
| `tax_id` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `status` | text |  | `'issued'::text` |  |
| `issued_by` | uuid | ✓ | `auth.uid()` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* **UNIQUE** `inv_order_once_idx` — `btree (order_id) WHERE ((order_id IS NOT NULL) AND (contract_id IS NULL) AND (status = 'issued'::text))`
* `invoices_contract_id_idx` — `btree (contract_id)`
* **UNIQUE** `invoices_no_uniq` — `btree (invoice_no) WHERE (status = 'issued'::text)`
* `invoices_order_id_idx` — `btree (order_id)`
* `invoices_ym_idx` — `btree (ym)`


---

# 支出 ① 請款到支出

需求 → 請款單 → 核准 → 支出 → 出款

## `purchase_demands`

採購需求單（想買什麼，還沒報價）

主鍵 `id`　・　10 欄　・　大約 6 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `demand_no` | text | ✓ |  |  |
| `requester_id` | uuid |  |  | → `profiles.id` (NO ACTION) |
| `requested_on` | date |  | `((now() AT TIME ZONE 'Asia/Taipei'::text))::date` |  |
| `note` | text | ✓ |  |  |
| `status` | text |  | `'open'::text` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |
| `ship_to` | text | ✓ |  |  |
| `ship_floor` | text | ✓ |  |  |

索引：

* **UNIQUE** `purchase_demands_demand_no_key` — `btree (demand_no)`


## `purchase_demand_items`

需求單的品項。`qty` 是 text —— 「3 箱」「一組」都填得進去

主鍵 `id`　・　16 欄　・　大約 8 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `demand_id` | uuid |  |  | → `purchase_demands.id` (CASCADE) |
| `item_name` | text |  |  |  |
| `spec` | text | ✓ |  |  |
| `qty` | text | ✓ |  |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `unit_price_est` | numeric | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `status` | text |  | `'pending'::text` |  |
| `request_item_id` | uuid | ✓ |  | → `purchase_request_items.id` (SET NULL) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `buy_link` | text | ✓ |  |  |
| `purpose_type` | text |  | `'estate'::text` |  |
| `purchased_on` | date | ✓ |  |  |
| `eta` | date | ✓ |  |  |
| `platform` | text | ✓ |  |  |

索引：

* `pdi_demand_idx` — `btree (demand_id)`
* `pdi_reqitem_idx` — `btree (request_item_id) WHERE (request_item_id IS NOT NULL)`
* `pdi_status_idx` — `btree (status) WHERE (status = ANY (ARRAY['pending'::text, 'quoted'::text]))`


## `purchase_requests`

**請款單**。走「送出 → 經理核 → 會計核 → 付款」

主鍵 `id`　・　35 欄　・　大約 110 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `req_no` | text |  |  |  |
| `requester_id` | uuid |  |  | → `users.id` (NO ACTION) |
| `status` | text |  | `'draft'::text` |  |
| `total_amount` | numeric |  | `0` |  |
| `payment_method` | text | ✓ |  |  |
| `payee_bank_code` | text | ✓ |  |  |
| `payee_account` | text | ✓ |  |  |
| `payee_company` | text | ✓ |  |  |
| `payee_tax_id` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `submitted_at` | timestamp with time zone | ✓ |  |  |
| `manager_approved_by` | uuid | ✓ |  | → `users.id` (NO ACTION) |
| `manager_approved_at` | timestamp with time zone | ✓ |  |  |
| `admin_approved_by` | uuid | ✓ |  | → `users.id` (NO ACTION) |
| `admin_approved_at` | timestamp with time zone | ✓ |  |  |
| `rejected_by` | uuid | ✓ |  | → `users.id` (NO ACTION) |
| `rejected_at` | timestamp with time zone | ✓ |  |  |
| `reject_reason` | text | ✓ |  |  |
| `purchased_on` | date | ✓ |  |  |
| `expense_generated_at` | timestamp with time zone | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `planned_transfer_on` | date | ✓ |  |  |
| `payout_account` | text | ✓ |  |  |
| `currency` | text |  | `'TWD'::text` |  |
| `fx_rate` | numeric |  | `1` |  |
| `voucher_no` | text | ✓ |  |  |
| `no_voucher` | boolean |  | `false` |  |
| `fee_mode` | text |  | `'included'::text` |  |
| `fee_amount` | numeric |  | `0` |  |
| `shared_voucher` | boolean |  | `false` |  |
| `book` | text |  | `'anxing'::text` |  |
| `advance_category` | text | ✓ |  |  |
| `advance_usage` | text | ✓ |  |  |
| `advance_for_book` | text | ✓ |  |  |

索引：

* `pr_book_idx` — `btree (book)`
* `pr_created_idx` — `btree (created_at DESC)`
* `pr_planned_idx` — `btree (planned_transfer_on)`
* `pr_requester_idx` — `btree (requester_id)`
* `pr_status_idx` — `btree (status)`
* **UNIQUE** `purchase_requests_req_no_key` — `btree (req_no)`


## `purchase_request_items`

請款單的品項，每一項有自己的會計科目與用途

主鍵 `id`　・　13 欄　・　大約 298 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `request_id` | uuid |  |  | → `purchase_requests.id` (CASCADE) |
| `item_name` | text |  |  |  |
| `amount` | numeric | ✓ |  |  |
| `account_code` | text | ✓ |  | → `account_codes.code` (NO ACTION) |
| `purpose_type` | text |  | `'estate'::text` |  |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `note` | text | ✓ |  |  |
| `sort` | integer |  | `0` |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `amount_original` | numeric | ✓ |  |  |
| `voucher_no` | text | ✓ |  |  |
| `no_voucher` | boolean |  | `false` |  |

索引：

* `pri_estate_idx` — `btree (estate_id)`
* `pri_request_idx` — `btree (request_id)`


## `expenses`

**支出**。`deferred` 是遞延認列、`parent_expense_id` 是遞延的子單、`book` 分事業體

主鍵 `id`　・　32 欄　・　大約 419 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `spent_on` | date |  |  |  |
| `item_name` | text |  |  |  |
| `amount` | numeric |  | `0` |  |
| `account_code` | text | ✓ |  | → `account_codes.code` (NO ACTION) |
| `purpose_type` | text |  | `'estate'::text` |  |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `voucher_no` | text | ✓ |  |  |
| `payment_method` | text | ✓ |  |  |
| `pay_account` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `source_item_id` | uuid | ✓ |  | → `purchase_request_items.id` (SET NULL) |
| `created_by` | uuid | ✓ |  | → `users.id` (NO ACTION) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `currency` | text |  | `'TWD'::text` |  |
| `fx_rate` | numeric |  | `1` |  |
| `amount_original` | numeric | ✓ |  |  |
| `no_voucher` | boolean |  | `false` |  |
| `request_id` | uuid | ✓ |  | → `purchase_requests.id` (SET NULL) |
| `fee_request_id` | uuid | ✓ |  | → `purchase_requests.id` (SET NULL) |
| `parent_expense_id` | uuid | ✓ |  | → `expenses.id` (CASCADE) |
| `gross_amount` | numeric | ✓ |  |  |
| `deferred` | boolean |  | `false` |  |
| `starred` | boolean |  | `false` |  |
| `book` | text |  | `'anxing'::text` |  |
| `fee_payment_id` | uuid | ✓ |  | → `order_payments.id` (CASCADE) |
| `non_operating` | boolean |  | `false` |  |
| `tags` | text[] |  | `'{}'::text[]` |  |
| `hk_job_key` | text | ✓ |  |  |
| `hk_labor_key` | text | ✓ |  |  |
| `advance_id` | uuid | ✓ |  | → `advance_payments.id` (SET NULL) |

索引：

* `exp_account_idx` — `btree (account_code)`
* `exp_estate_idx` — `btree (estate_id)`
* `exp_parent_idx` — `btree (parent_expense_id)`
* `exp_prop_idx` — `btree (property_id)`
* `exp_request_idx` — `btree (request_id)`
* `exp_spent_idx` — `btree (spent_on DESC)`
* `exp_src_idx` — `btree (source_item_id)`
* `exp_starred_idx` — `btree (starred) WHERE starred`
* `expenses_book_idx` — `btree (book)`
* **UNIQUE** `expenses_fee_payment_uidx` — `btree (fee_payment_id)`
* **UNIQUE** `expenses_fee_request_uidx` — `btree (fee_request_id)`
* **UNIQUE** `expenses_hk_job_uniq` — `btree (hk_job_key)`
* **UNIQUE** `expenses_hk_labor_uniq` — `btree (hk_labor_key)`
* `expenses_non_operating_idx` — `btree (spent_on) WHERE non_operating`
* **UNIQUE** `expenses_source_item_id_key` — `btree (source_item_id)`
* **UNIQUE** `expenses_source_item_uidx` — `btree (source_item_id)`
* `expenses_tags_idx` — `gin (tags)`
* **UNIQUE** `uq_expense_fee_request` — `btree (fee_request_id) WHERE (fee_request_id IS NOT NULL)`


## `other_book_payments`

其他收支帳的實支紀錄（愛皮／洪鯊那本）

主鍵 `id`　・　9 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `expense_id` | uuid |  |  | → `expenses.id` (CASCADE) |
| `paid_on` | date |  |  |  |
| `amount` | numeric |  |  |  |
| `method` | text | ✓ |  |  |
| `account` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `created_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `created_at` | timestamp with time zone |  | `clock_timestamp()` |  |

索引：

* `obp_expense_idx` — `btree (expense_id, paid_on)`


---

# 憑證附件

一張 attachments 掛在九種單據上

## `attachments`

憑證附件。一張表掛九種單據，每一種一個 `*_id` 欄位（同時只會有一個有值）

主鍵 `id`　・　16 欄　・　大約 275 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `request_id` | uuid | ✓ |  | → `purchase_requests.id` (CASCADE) |
| `expense_id` | uuid | ✓ |  | → `expenses.id` (CASCADE) |
| `path` | text |  |  |  |
| `file_name` | text | ✓ |  |  |
| `mime_type` | text | ✓ |  |  |
| `size_bytes` | integer | ✓ |  |  |
| `uploaded_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `deposit_id` | uuid | ✓ |  | → `deposits.id` (CASCADE) |
| `order_payment_id` | uuid | ✓ |  | → `order_payments.id` (CASCADE) |
| `deposit_payment_id` | uuid | ✓ |  | → `deposit_payments.id` (CASCADE) |
| `request_item_id` | uuid | ✓ |  | → `purchase_request_items.id` (CASCADE) |
| `tender_id` | uuid | ✓ |  | → `tenders.id` (CASCADE) |
| `order_id` | uuid | ✓ |  | → `orders.id` (CASCADE) |
| `bank_transaction_id` | uuid | ✓ |  | → `bank_transactions.id` (CASCADE) |

索引：

* `att_bank_txn_idx` — `btree (bank_transaction_id) WHERE (bank_transaction_id IS NOT NULL)`
* `att_dep_idx` — `btree (deposit_id)`
* `att_dep_pay_idx` — `btree (deposit_payment_id)`
* `att_exp_idx` — `btree (expense_id)`
* `att_op_idx` — `btree (order_payment_id)`
* `att_req_idx` — `btree (request_id)`
* `att_req_item_idx` — `btree (request_item_id)`
* `att_tender_idx` — `btree (tender_id) WHERE (tender_id IS NOT NULL)`
* `attachments_order_id_idx` — `btree (order_id) WHERE (order_id IS NOT NULL)`
* **UNIQUE** `attachments_path_key` — `btree (path)`


---

# 支出 ② 暫付與代碼主檔

先墊的錢，以及會計科目／收付款方式

## `advance_payments`

**暫付** —— 先墊出去的錢（押金、代墊），還沒還回來

主鍵 `id`　・　20 欄　・　大約 5 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `category` | text |  |  |  |
| `counterparty` | text |  |  |  |
| `usage` | text |  |  |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (SET NULL) |
| `amount` | numeric(14,2) |  |  |  |
| `paid_on` | date | ✓ |  |  |
| `paid_account` | text | ✓ |  |  |
| `refunded_on` | date | ✓ |  |  |
| `refunded_amount` | numeric(14,2) | ✓ |  |  |
| `refund_account` | text | ✓ |  |  |
| `forfeit_expense_id` | uuid | ✓ |  | → `expenses.id` (SET NULL) |
| `note` | text | ✓ |  |  |
| `created_by` | uuid | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |
| `request_id` | uuid | ✓ |  | → `purchase_requests.id` (SET NULL) |
| `purpose_type` | text |  | `'estate'::text` |  |
| `for_book` | text | ✓ |  |  |
| `source_item_id` | uuid | ✓ |  | → `purchase_request_items.id` (SET NULL) |

索引：

* **UNIQUE** `ap_item_uniq` — `btree (source_item_id) WHERE (source_item_id IS NOT NULL)`
* `ap_open_idx` — `btree (paid_on) WHERE ((paid_on IS NOT NULL) AND (refunded_on IS NULL))`
* `ap_paid_idx` — `btree (paid_on DESC)`
* **UNIQUE** `ap_request_uniq` — `btree (request_id) WHERE ((request_id IS NOT NULL) AND (category <> '代墊'::text))`

⚠ **刪一筆會被 1 條外鍵擋住**：`advance_repayment_lines.advance_id`


## `advance_repayments`

暫付的還款（一次還可以沖掉好幾筆）

主鍵 `id`　・　10 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `counterparty` | text |  |  |  |
| `for_book` | text | ✓ |  |  |
| `repaid_on` | date |  |  |  |
| `amount` | numeric |  |  |  |
| `in_account` | text |  |  |  |
| `out_account` | text |  |  |  |
| `note` | text | ✓ |  |  |
| `created_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `created_at` | timestamp with time zone |  | `clock_timestamp()` |  |


## `advance_repayment_lines`

還款沖掉了哪幾筆暫付、各沖多少

主鍵 `id`　・　5 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `repayment_id` | uuid |  |  | → `advance_repayments.id` (CASCADE) |
| `advance_id` | uuid |  |  | → `advance_payments.id` (RESTRICT) |
| `amount` | numeric |  |  |  |
| `created_at` | timestamp with time zone |  | `clock_timestamp()` |  |

索引：

* `arl_advance_idx` — `btree (advance_id, created_at)`
* **UNIQUE** `arl_once_uniq` — `btree (repayment_id, advance_id)`


## `account_codes`

會計科目。`kind` 分收入／支出，`book` 分事業體

主鍵 `code`　・　6 欄　・　大約 60 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`code`** 🔑 | text |  |  |  |
| `name` | text |  |  |  |
| `sort` | integer |  | `0` |  |
| `active` | boolean |  | `true` |  |
| `kind` | text |  | `'expense'::text` |  |
| `book` | text |  | `'anxing'::text` |  |

⚠ **刪一筆會被 3 條外鍵擋住**：`expenses.account_code`、`orders.account_code`、`purchase_request_items.account_code`


## `payment_accounts`

收付款方式（現金、各家銀行帳戶…）。`code` 是外鍵鍵值

主鍵 `id`　・　10 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `method` | text |  |  |  |
| `code` | text |  |  |  |
| `name` | text |  |  |  |
| `for_income` | boolean |  | `true` |  |
| `for_payment` | boolean |  | `true` |  |
| `sort` | integer |  | `50` |  |
| `active` | boolean |  | `true` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `book` | text |  | `'anxing'::text` |  |

索引：

* `pay_acct_method_idx` — `btree (method)`
* **UNIQUE** `payment_accounts_code_key` — `btree (code)`

⚠ **刪一筆會被 2 條外鍵擋住**：`deposit_payments.account`、`order_payments.account`


## `payee_presets`

常用收款人（銀行代碼、帳號、統編），請款單上下拉選

主鍵 `id`　・　11 欄　・　大約 27 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `label` | text |  |  |  |
| `bank_code` | text | ✓ |  |  |
| `account` | text |  |  |  |
| `company` | text | ✓ |  |  |
| `tax_id` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `sort` | integer |  | `0` |  |
| `active` | boolean |  | `true` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |

索引：

* **UNIQUE** `payee_presets_account_uniq` — `btree (account) WHERE active`
* `payee_presets_sort_idx` — `btree (sort, label)`


---

# 銀行與稅

對帳單、發票、營業稅

## `bank_accounts`

銀行帳戶主檔。`manual_entry` 標「這本是手動記帳不是對帳單匯入」

主鍵 `id`　・　13 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `name` | text |  |  |  |
| `bank` | text |  |  |  |
| `account_no` | text | ✓ |  |  |
| `account_no_tail` | text | ✓ |  |  |
| `parser` | text | ✓ |  |  |
| `opening_balance` | numeric(14,2) | ✓ |  |  |
| `opening_balance_on` | date | ✓ |  |  |
| `sort` | integer |  | `0` |  |
| `active` | boolean |  | `true` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `kind` | text |  | `'bank'::text` |  |
| `manual_entry` | boolean |  | `false` |  |

索引：

* **UNIQUE** `uq_bank_accounts_tail` — `btree (account_no_tail)`


## `bank_statements`

上傳過的對帳單（一個檔案一筆）

主鍵 `id`　・　15 欄　・　大約 40 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `account_id` | uuid |  |  | → `bank_accounts.id` (CASCADE) |
| `period_from` | date |  |  |  |
| `period_to` | date |  |  |  |
| `closing_balance` | numeric(14,2) | ✓ |  |  |
| `total_debit` | numeric(14,2) | ✓ |  |  |
| `total_credit` | numeric(14,2) | ✓ |  |  |
| `parsed_count` | integer |  | `0` |  |
| `inserted_count` | integer |  | `0` |  |
| `skipped_count` | integer |  | `0` |  |
| `warnings` | text[] | ✓ |  |  |
| `file_name` | text | ✓ |  |  |
| `uploaded_by` | uuid | ✓ |  | → `users.id` (NO ACTION) |
| `uploaded_at` | timestamp with time zone |  | `now()` |  |
| `note` | text | ✓ |  |  |

索引：

* `idx_bank_statements_acct` — `btree (account_id, period_to DESC)`


## `bank_transactions`

銀行交易明細。`balance` 由觸發器維護（`migration_203` 之後），`bank_balance` 是對帳單上原本印的

主鍵 `id`　・　17 欄　・　大約 2,088 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `account_id` | uuid |  |  | → `bank_accounts.id` (CASCADE) |
| `statement_id` | uuid | ✓ |  | → `bank_statements.id` (SET NULL) |
| `seq` | integer | ✓ |  |  |
| `txn_date` | date | ✓ |  |  |
| `post_date` | date |  |  |  |
| `txn_time` | time without time zone | ✓ |  |  |
| `description` | text | ✓ |  |  |
| `counterparty` | text | ✓ |  |  |
| `debit` | numeric(14,2) |  | `0` |  |
| `credit` | numeric(14,2) |  | `0` |  |
| `balance` | numeric(14,2) |  |  |  |
| `memo` | text | ✓ |  |  |
| `ref_no` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `bank_balance` | numeric(14,2) | ✓ |  |  |
| `balance_note` | text | ✓ |  |  |

索引：

* `idx_bank_txn_list` — `btree (account_id, post_date DESC, seq DESC)`
* **UNIQUE** `uq_bank_txn` — `btree (account_id, post_date, bank_balance, COALESCE(txn_time, '00:00:00'::time without time zone))`


## `tax_invoice`

**稅務**的進銷項發票（跟 `invoices` 不是同一件事）

主鍵 `id`　・　24 欄　・　大約 72 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `period` | text |  |  |  |
| `company_tax_id` | text |  |  |  |
| `kind` | text |  |  |  |
| `category` | text | ✓ |  |  |
| `tax_code` | text | ✓ |  |  |
| `invoice_date` | date |  |  |  |
| `invoice_no` | text |  |  |  |
| `counterparty` | text | ✓ |  |  |
| `counterparty_tax_id` | text | ✓ |  |  |
| `summary` | text | ✓ |  |  |
| `item_name` | text | ✓ |  |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `net_amount` | numeric(14,0) |  | `0` |  |
| `tax_amount` | numeric(14,0) |  | `0` |  |
| `total_amount` | numeric(14,0) |  | `0` |  |
| `voucher_ref` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `voided` | boolean |  | `false` |  |
| `source` | text |  | `'manual'::text` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ |  |  |
| `expense_id` | uuid | ✓ |  | → `expenses.id` (SET NULL) |

索引：

* `tax_invoice_date_idx` — `btree (invoice_date)`
* **UNIQUE** `tax_invoice_expense_uniq` — `btree (expense_id) WHERE (expense_id IS NOT NULL)`
* `tax_invoice_period_idx` — `btree (company_tax_id, period, kind)`
* **UNIQUE** `tax_invoice_uniq` — `btree (company_tax_id, kind, invoice_no)`


## `tax_period`

營業稅期別（兩個月一期），留期初留抵與應納稅額

主鍵 `id`　・　13 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `period` | text |  |  |  |
| `company_tax_id` | text |  |  |  |
| `status` | text |  | `'open'::text` |  |
| `carry_in` | numeric(14,0) |  | `0` |  |
| `out_tax` | numeric(14,0) | ✓ |  |  |
| `in_tax` | numeric(14,0) | ✓ |  |  |
| `payable` | numeric(14,0) | ✓ |  |  |
| `carry_out` | numeric(14,0) | ✓ |  |  |
| `closed_at` | timestamp with time zone | ✓ |  |  |
| `closed_by` | uuid | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* **UNIQUE** `tax_period_uniq` — `btree (company_tax_id, period)`


## `accounting_reports`

外部會計師給的報表檔案

主鍵 `id`　・　14 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `kind` | text |  |  |  |
| `period_start` | date | ✓ |  |  |
| `title` | text |  |  |  |
| `uploaded_on` | date | ✓ | `CURRENT_DATE` |  |
| `note` | text | ✓ |  |  |
| `file_path` | text | ✓ |  |  |
| `file_name` | text | ✓ |  |  |
| `file_size` | bigint | ✓ |  |  |
| `created_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `updated_at` | timestamp with time zone |  | `clock_timestamp()` |  |
| `filed_on` | date | ✓ |  |  |

索引：

* `ar_kind_period_idx` — `btree (kind, period_start DESC)`


---

# 房務 ① 排班與工單

TimeTree 匯入 → 工單 → 拆帳

## `hk_event`

從 TimeTree 匯進來的原始行程（還沒變成工單）

主鍵 `id`　・　13 欄　・　大約 235 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `period` | text |  |  |  |
| `event_date` | date |  |  |  |
| `title` | text |  |  |  |
| `label` | text | ✓ |  |  |
| `assignees` | text[] |  | `'{}'::text[]` |  |
| `external_id` | text | ✓ |  |  |
| `parsed_code` | text | ✓ |  |  |
| `work_type` | text | ✓ |  |  |
| `excluded` | text | ✓ |  |  |
| `imported_at` | timestamp with time zone |  | `now()` |  |
| `dismissed_at` | timestamp with time zone | ✓ |  |  |
| `dismissed_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |

索引：

* **UNIQUE** `hk_event_period_event_date_title_external_id_key` — `btree (period, event_date, title, external_id)`
* `hk_event_period_idx` — `btree (period, event_date)`
* `hk_event_todo_idx` — `btree (period, event_date) WHERE (dismissed_at IS NULL)`


## `hk_work_item`

房務**工單** —— 誰、哪天、哪個房源、做什麼。`units_override`／`points_override`／`amount_override` 是覆寫的間數／點數／金額

主鍵 `id`　・　14 欄　・　大約 184 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `event_id` | uuid | ✓ |  | → `hk_event.id` (SET NULL) |
| `period` | text |  |  |  |
| `work_date` | date |  |  |  |
| `property_code` | text | ✓ |  |  |
| `work_type` | text |  | `'清潔'::text` |  |
| `staff_id` | uuid |  |  | → `hk_staff.id` (NO ACTION) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `source` | text |  | `'timetree'::text` |  |
| `note` | text | ✓ |  |  |
| `version` | integer |  | `1` |  |
| `units_override` | numeric(6,2) | ✓ |  |  |
| `points_override` | numeric(8,2) | ✓ |  |  |
| `amount_override` | numeric(14,2) | ✓ |  |  |

索引：

* `hk_wi_period_idx` — `btree (period, work_date)`
* `hk_wi_prop_idx` — `btree (period, property_code)`


## `hk_work_split`

**拆帳** —— 一份工的錢記到好幾間房源

主鍵 `id`　・　10 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `period` | text |  |  |  |
| `work_date` | date |  |  |  |
| `job_code` | text |  |  |  |
| `work_type` | text |  |  |  |
| `property_id` | uuid |  |  | → `properties.id` (NO ACTION) |
| `amount` | numeric(14,2) |  |  |  |
| `seq` | integer |  | `0` |  |
| `note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* `hk_work_split_period_idx` — `btree (period, work_date)`
* `hk_work_split_property_idx` — `btree (property_id)`
* **UNIQUE** `hk_work_split_uniq` — `btree (work_date, job_code, work_type, property_id)`


## `hk_task`

排班表上的一格（含系統自動產生的清潔工單，`auto_kind` 標來源）

主鍵 `id`　・　16 欄　・　大約 422 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `work_date` | date |  |  |  |
| `property_id` | uuid | ✓ |  | → `properties.id` (SET NULL) |
| `work_type` | text |  | `'清潔'::text` |  |
| `staff_id` | uuid | ✓ |  | → `staff.id` (SET NULL) |
| `order_id` | uuid | ✓ |  | → `orders.id` (CASCADE) |
| `auto_kind` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `done_at` | timestamp with time zone | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ |  |  |
| `accepted` | boolean |  | `true` |  |
| `all_day` | boolean |  | `true` |  |
| `start_time` | time without time zone | ✓ |  |  |
| `end_time` | time without time zone | ✓ |  |  |
| `title` | text | ✓ |  |  |

索引：

* `hk_task_pending_idx` — `btree (work_date) WHERE (NOT accepted)`
* `hk_task_time_idx` — `btree (work_date, start_time NULLS FIRST)`
* `idx_hk_task_date` — `btree (work_date)`
* `idx_hk_task_staff` — `btree (staff_id, work_date)`
* **UNIQUE** `uq_hk_task_auto` — `btree (order_id, auto_kind) WHERE ((order_id IS NOT NULL) AND (auto_kind IS NOT NULL))`


## `hk_day`

某人某天的出勤狀態（請假、時數）

主鍵 `work_date, staff_id`　・　7 欄　・　大約 56 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| `period` | text |  |  |  |
| **`work_date`** 🔑 | date |  |  |  |
| **`staff_id`** 🔑 | uuid |  |  | → `hk_staff.id` (CASCADE) |
| `status` | text | ✓ |  |  |
| `hours` | numeric | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `rooms_override` | numeric(6,2) | ✓ |  |  |


## `hk_staff`

房務人員（跟 `staff` 對應，`count_mode` 決定怎麼算她的量）

主鍵 `id`　・　14 欄　・　大約 9 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `code` | text |  |  |  |
| `name` | text |  |  |  |
| `count_mode` | text |  | `'none'::text` |  |
| `count_cleans` | boolean |  | `true` |  |
| `color` | text | ✓ |  |  |
| `leave_prefix` | text | ✓ |  |  |
| `active` | boolean |  | `true` |  |
| `sort` | integer |  | `0` |  |
| `source_names` | text[] |  | `'{}'::text[]` |  |
| `color_text` | text | ✓ |  |  |
| `color_bar` | text | ✓ |  |  |
| `staff_id` | uuid | ✓ |  | → `staff.id` (NO ACTION) |
| `hourly_rate` | numeric(10,2) | ✓ |  |  |

索引：

* **UNIQUE** `hk_staff_staff_id_uniq` — `btree (staff_id) WHERE (staff_id IS NOT NULL)`

⚠ **刪一筆會被 1 條外鍵擋住**：`hk_work_item.staff_id`


## `hk_property`

房務用的房源代碼與別名（跟 `properties` 對應）

主鍵 `id`　・　11 欄　・　大約 70 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `code` | text |  |  |  |
| `name` | text | ✓ |  |  |
| `aliases` | text[] |  | `'{}'::text[]` |  |
| `beds` | integer | ✓ |  |  |
| `linen_group` | text |  | `'other'::text` |  |
| `active` | boolean |  | `true` |  |
| `sort` | integer |  | `0` |  |
| `count_linen` | boolean |  | `true` |  |
| `ptype` | text |  | `'room'::text` |  |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |

索引：

* **UNIQUE** `hk_property_code_key` — `btree (code)`
* `hk_property_property_id_idx` — `btree (property_id) WHERE (property_id IS NOT NULL)`
* **UNIQUE** `hk_property_property_id_uniq` — `btree (property_id) WHERE (property_id IS NOT NULL)`


---

# 房務 ② 設定、人事費與評價

每月設定、人事費攤提、清潔記錄與 Airbnb 評價

## `hk_period`

每個月的房務設定（怎麼算量、含不含贈送）

主鍵 `period`　・　5 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`period`** 🔑 | text |  |  |  |
| `count_mode` | text |  | `'clean'::text` |  |
| `include_gift` | boolean |  | `true` |  |
| `note` | text | ✓ |  |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |


## `hk_month_property`

某個月某個房源的覆寫（間數、布巾取用）

主鍵 `period, property_code`　・　4 欄　・　大約 23 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`period`** 🔑 | text |  |  |  |
| **`property_code`** 🔑 | text |  |  |  |
| `count_override` | integer | ✓ |  |  |
| `linen_taken` | integer |  | `0` |  |


## `hk_setting`

房務的全域設定

主鍵 `key`　・　6 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`key`** 🔑 | text |  |  |  |
| `value` | text | ✓ |  |  |
| `vtype` | text |  | `'text'::text` |  |
| `options` | text[] | ✓ |  |  |
| `description` | text | ✓ |  |  |
| `sort` | integer |  | `0` |  |


## `hk_work_type`

工作類型（清潔、退房、布巾…）與它算不算量

主鍵 `code`　・　6 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`code`** 🔑 | text |  |  |  |
| `name` | text |  |  |  |
| `count_workload` | boolean |  | `true` |  |
| `count_linen` | boolean |  | `true` |  |
| `sort` | integer |  | `0` |  |
| `active` | boolean |  | `true` |  |


## `hk_labor_cost`

房務人事費的月攤提（掛在物業或房源上）

主鍵 `id`　・　7 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (CASCADE) |
| `property_id` | uuid | ✓ |  | → `properties.id` (CASCADE) |
| `monthly_amount` | numeric(12,2) |  |  |  |
| `note` | text | ✓ |  |  |
| `active` | boolean |  | `true` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* **UNIQUE** `hklc_estate_uniq` — `btree (estate_id) WHERE (active AND (estate_id IS NOT NULL))`
* **UNIQUE** `hklc_property_uniq` — `btree (property_id) WHERE (active AND (property_id IS NOT NULL))`


## `hk_audit`

房務資料的異動紀錄

主鍵 `id`　・　7 欄　・　大約 104 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | bigint |  | `nextval('hk_audit_id_seq'::regclass)` |  |
| `table_name` | text |  |  |  |
| `record_key` | text |  |  |  |
| `action` | text |  |  |  |
| `changes` | jsonb | ✓ |  |  |
| `user_id` | uuid | ✓ |  |  |
| `at` | timestamp with time zone |  | `now()` |  |

索引：

* `hk_audit_at_idx` — `btree (at DESC)`


## `cleaning_records`

清潔檢查記錄（評分、備註、文件連結）

主鍵 `id`　・　14 欄　・　大約 172 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `record_key` | text |  |  |  |
| `record_date` | date |  |  |  |
| `staff_name` | text |  |  |  |
| `staff_id` | uuid | ✓ |  | → `staff.id` (NO ACTION) |
| `staff_type` | text | ✓ |  |  |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `property_raw` | text | ✓ |  |  |
| `estate_name` | text | ✓ |  |  |
| `overall_rating` | integer | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `doc_url` | text | ✓ |  |  |
| `source` | text | ✓ | `'seed'::text` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* **UNIQUE** `cleaning_records_record_key_key` — `btree (record_key)`
* `idx_clean_date` — `btree (record_date)`
* `idx_clean_note_trgm` — `gin (note gin_trgm_ops)`
* `idx_clean_property` — `btree (property_id)`
* `idx_clean_rating` — `btree (overall_rating)`
* `idx_clean_staff` — `btree (staff_id)`


## `reviews`

Airbnb 評價。`hidden_at` 是人工隱藏

主鍵 `id`　・　26 欄　・　大約 1,610 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `airbnb_review_id` | text |  |  |  |
| `property_id` | uuid | ✓ |  | → `properties.id` (NO ACTION) |
| `listing_name_raw` | text | ✓ |  |  |
| `guest_name` | text |  |  |  |
| `checkin_date` | date | ✓ |  |  |
| `checkout_date` | date | ✓ |  |  |
| `nights` | integer | ✓ |  |  |
| `overall_rating` | numeric(2,1) |  |  |  |
| `comment` | text | ✓ |  |  |
| `comment_original` | text | ✓ |  |  |
| `comment_language` | text | ✓ |  |  |
| `rating_checkin` | integer | ✓ |  |  |
| `rating_cleanliness` | integer | ✓ |  |  |
| `rating_accuracy` | integer | ✓ |  |  |
| `rating_communication` | integer | ✓ |  |  |
| `rating_location` | integer | ✓ |  |  |
| `rating_value` | integer | ✓ |  |  |
| `detail_comments` | jsonb | ✓ |  |  |
| `host_reply` | text | ✓ |  |  |
| `source_url` | text | ✓ |  |  |
| `scraped_at` | timestamp with time zone |  | `now()` |  |
| `imported_via` | text | ✓ | `'csv'::text` |  |
| `hidden_at` | timestamp with time zone | ✓ |  |  |
| `hidden_by` | uuid | ✓ |  |  |
| `hidden_reason` | text | ✓ |  |  |

索引：

* `idx_reviews_checkout` — `btree (checkout_date)`
* `idx_reviews_property` — `btree (property_id)`
* `idx_reviews_rating` — `btree (overall_rating)`
* **UNIQUE** `reviews_airbnb_review_id_key` — `btree (airbnb_review_id)`
* `reviews_visible_idx` — `btree (checkout_date DESC) WHERE (hidden_at IS NULL)`


---

# 備品

取用／補貨／盤點 —— 餘量是算出來的

## `supply_item`

備品品項（哪個物業、規格、廠商、效期）

主鍵 `id`　・　11 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `name` | text |  |  |  |
| `spec` | text | ✓ |  |  |
| `vendor` | text | ✓ |  |  |
| `expire_on` | date | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `active` | boolean |  | `true` |  |
| `sort` | integer |  | `0` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ |  |  |

索引：

* **UNIQUE** `supply_item_uniq` — `btree (estate_id, name, spec) NULLS NOT DISTINCT`


## `supply_txn`

備品異動:**取用**（−）與**補貨**（＋）。★ 餘量是這張表加總出來的，沒有存成欄位

主鍵 `id`　・　9 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `item_id` | uuid |  |  | → `supply_item.id` (CASCADE) |
| `kind` | text |  |  |  |
| `qty` | numeric(14,2) |  |  |  |
| `happened_on` | date |  | `((now() AT TIME ZONE 'Asia/Taipei'::text))::date` |  |
| `note` | text | ✓ |  |  |
| `count_id` | uuid | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ |  |  |

索引：

* `supply_txn_item_idx` — `btree (item_id, happened_on)`


## `supply_count`

月底**盤點**:系統算出來多少 vs 實際數到多少

主鍵 `id`　・　9 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `item_id` | uuid |  |  | → `supply_item.id` (CASCADE) |
| `ym` | text |  |  |  |
| `system_qty` | numeric(14,2) |  |  |  |
| `counted_qty` | numeric(14,2) |  |  |  |
| `diff` | numeric(14,2) |  |  |  |
| `reason` | text | ✓ |  |  |
| `counted_at` | timestamp with time zone |  | `now()` |  |
| `counted_by` | uuid | ✓ |  |  |

索引：

* **UNIQUE** `supply_count_uniq` — `btree (item_id, ym)`


---

# 人事與出勤

打卡、請假、加班

## `attendance`

打卡紀錄（含 GPS 與距離，判斷有沒有在範圍內）

主鍵 `id`　・　22 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `user_id` | uuid |  |  | → `profiles.id` (CASCADE) |
| `work_date` | date |  |  |  |
| `in_at` | timestamp with time zone | ✓ |  |  |
| `in_lat` | numeric | ✓ |  |  |
| `in_lng` | numeric | ✓ |  |  |
| `in_estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `in_distance_m` | numeric | ✓ |  |  |
| `in_in_range` | boolean | ✓ |  |  |
| `out_at` | timestamp with time zone | ✓ |  |  |
| `out_lat` | numeric | ✓ |  |  |
| `out_lng` | numeric | ✓ |  |  |
| `out_estate_id` | uuid | ✓ |  | → `estates.id` (NO ACTION) |
| `out_distance_m` | numeric | ✓ |  |  |
| `out_in_range` | boolean | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `edited_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |
| `status` | text |  | `'normal'::text` |  |
| `late_min` | integer | ✓ |  |  |
| `early_min` | integer | ✓ |  |  |

索引：

* `attendance_date_idx` — `btree (work_date)`
* **UNIQUE** `attendance_user_date_uniq` — `btree (user_id, work_date)`


## `attendance_fixes`

補打卡申請

主鍵 `id`　・　11 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `user_id` | uuid |  |  | → `profiles.id` (CASCADE) |
| `work_date` | date |  |  |  |
| `kind` | text |  |  |  |
| `fix_time` | time without time zone |  |  |  |
| `reason` | text |  |  |  |
| `status` | text |  | `'pending'::text` |  |
| `reviewed_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `reviewed_at` | timestamp with time zone | ✓ |  |  |
| `review_note` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* `att_fix_pending_idx` — `btree (status) WHERE (status = 'pending'::text)`
* `att_fix_user_idx` — `btree (user_id, work_date)`


## `leave_types`

假別

主鍵 `code`　・　7 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`code`** 🔑 | text |  |  |  |
| `name` | text |  |  |  |
| `has_quota` | boolean |  | `true` |  |
| `paid` | boolean |  | `true` |  |
| `sort` | integer |  | `0` |  |
| `active` | boolean |  | `true` |  |
| `note` | text | ✓ |  |  |

⚠ **刪一筆會被 2 條外鍵擋住**：`leave_balances.type_code`、`leave_requests.type_code`


## `leave_balances`

每人每年每個假別的額度與已用

主鍵 `id`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `user_id` | uuid |  |  | → `profiles.id` (CASCADE) |
| `year` | integer |  |  |  |
| `type_code` | text |  |  | → `leave_types.code` (NO ACTION) |
| `quota_hours` | numeric |  | `0` |  |
| `used_hours` | numeric |  | `0` |  |
| `note` | text | ✓ |  |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |

索引：

* **UNIQUE** `leave_bal_uniq` — `btree (user_id, year, type_code)`


## `leave_requests`

請假單

主鍵 `id`　・　15 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `user_id` | uuid |  |  | → `profiles.id` (CASCADE) |
| `type_code` | text |  |  | → `leave_types.code` (NO ACTION) |
| `start_at` | timestamp with time zone |  |  |  |
| `end_at` | timestamp with time zone |  |  |  |
| `hours` | numeric |  |  |  |
| `reason` | text | ✓ |  |  |
| `status` | text |  | `'pending'::text` |  |
| `manager_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `manager_at` | timestamp with time zone | ✓ |  |  |
| `admin_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `admin_at` | timestamp with time zone | ✓ |  |  |
| `reject_reason` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |

索引：

* `leave_no_overlap` — `gist (user_id, tstzrange(start_at, end_at)) WHERE (status = ANY (ARRAY['pending'::text, 'approved'::text]))`
* `leave_req_pending_idx` — `btree (status) WHERE (status = 'pending'::text)`
* `leave_req_range_idx` — `btree (start_at, end_at)`
* `leave_req_user_idx` — `btree (user_id, start_at)`


## `leave_seniority`

年資 → 特休天數的對照

主鍵 `threshold_months`　・　3 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`threshold_months`** 🔑 | integer |  |  |  |
| `days` | numeric |  |  |  |
| `note` | text | ✓ |  |  |


## `overtime_requests`

加班單

主鍵 `id`　・　13 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `user_id` | uuid |  |  | → `profiles.id` (CASCADE) |
| `work_date` | date |  |  |  |
| `start_at` | timestamp with time zone |  |  |  |
| `end_at` | timestamp with time zone |  |  |  |
| `hours` | numeric |  |  |  |
| `reason` | text |  |  |  |
| `status` | text |  | `'pending'::text` |  |
| `manager_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `manager_at` | timestamp with time zone | ✓ |  |  |
| `reject_reason` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |

索引：

* `ot_no_overlap` — `gist (user_id, tstzrange(start_at, end_at)) WHERE (status = ANY (ARRAY['pending'::text, 'approved'::text]))`
* `ot_pending_idx` — `btree (status) WHERE (status = 'pending'::text)`
* `ot_user_idx` — `btree (user_id, work_date)`


## `work_settings`

全站的上下班時間與每日工時（只有一列）

主鍵 `id`　・　7 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | integer |  | `1` |  |
| `work_hours_per_day` | numeric |  | `8` |  |
| `work_start` | time without time zone |  | `'09:00:00'::time without time zone` |  |
| `work_end` | time without time zone |  | `'18:00:00'::time without time zone` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |
| `punch_before_min` | integer |  | `120` |  |
| `punch_after_min` | integer |  | `120` |  |


## `holidays`

國定假日與補班日

主鍵 `d`　・　4 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`d`** 🔑 | date |  |  |  |
| `name` | text |  |  |  |
| `kind` | text |  | `'holiday'::text` |  |
| `note` | text | ✓ |  |  |


---

# 系統 ① 通知、稽核與回收桶

誰改了什麼、誰該被通知、刪掉的東西去哪

## `notifications`

站內通知

主鍵 `id`　・　8 欄　・　大約 64 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | bigint |  | `nextval('notifications_id_seq'::regclass)` |  |
| `user_id` | uuid |  |  | → `profiles.id` (CASCADE) |
| `kind` | text |  |  |  |
| `title` | text |  |  |  |
| `body` | text |  | `''::text` |  |
| `url` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `read_at` | timestamp with time zone | ✓ |  |  |

索引：

* `notifications_unread_idx` — `btree (user_id) WHERE (read_at IS NULL)`
* `notifications_user_idx` — `btree (user_id, created_at DESC)`


## `notification_prefs`

每個人要收哪幾類通知

主鍵 `user_id`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`user_id`** 🔑 | uuid |  |  | → `users.id` (CASCADE) |
| `orders` | boolean |  | `false` |  |
| `approvals` | boolean |  | `true` |  |
| `reviews` | boolean |  | `false` |  |
| `cleaning` | boolean |  | `false` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |
| `purchasing` | boolean |  | `true` |  |
| `board` | boolean |  | `true` |  |


## `push_subscriptions`

瀏覽器推播的訂閱端點

主鍵 `id`　・　9 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `user_id` | uuid |  |  | → `users.id` (CASCADE) |
| `endpoint` | text |  |  |  |
| `p256dh` | text |  |  |  |
| `auth` | text |  |  |  |
| `user_agent` | text | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `fail_count` | integer |  | `0` |  |
| `last_sent_at` | timestamp with time zone | ✓ |  |  |

索引：

* `push_sub_user_idx` — `btree (user_id)`
* **UNIQUE** `push_subscriptions_endpoint_key` — `btree (endpoint)`


## `data_audit`

**全站異動稽核** —— 誰在什麼時候改了哪一筆的哪一欄

主鍵 `id`　・　8 欄　・　大約 9,518 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | bigint |  | `nextval('data_audit_id_seq'::regclass)` |  |
| `at` | timestamp with time zone |  | `now()` |  |
| `user_id` | uuid | ✓ |  |  |
| `table_name` | text |  |  |  |
| `record_id` | uuid | ✓ |  |  |
| `label` | text | ✓ |  |  |
| `action` | text |  |  |  |
| `changes` | jsonb |  | `'{}'::jsonb` |  |

索引：

* `idx_data_audit_at` — `btree (at DESC)`
* `idx_data_audit_record` — `btree (record_id)`
* `idx_data_audit_table` — `btree (table_name, at DESC)`
* `idx_data_audit_user` — `btree (user_id, at DESC)`


## `trash`

**回收桶**。刪掉的東西連同子資料存成 jsonb，可以復原

主鍵 `id`　・　14 欄　・　大約 150 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `table_name` | text |  |  |  |
| `record_id` | uuid |  |  |  |
| `label` | text | ✓ |  |  |
| `payload` | jsonb | ✓ |  |  |
| `children` | jsonb |  | `'[]'::jsonb` |  |
| `child_count` | integer |  | `0` |  |
| `reason` | text | ✓ |  |  |
| `deleted_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `deleted_at` | timestamp with time zone |  | `now()` |  |
| `restored_at` | timestamp with time zone | ✓ |  |  |
| `restored_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `purged_at` | timestamp with time zone | ✓ |  |  |
| `purged_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |

索引：

* `trash_at_idx` — `btree (deleted_at DESC)`
* `trash_open_idx` — `btree (deleted_at DESC) WHERE ((restored_at IS NULL) AND (purged_at IS NULL))`
* `trash_table_idx` — `btree (table_name, deleted_at DESC)`


## `schema_migrations`

跑過哪幾支 migration。★ 有洞 —— 沒呼叫 `record_migration()` 的那幾支不在裡面

主鍵 `name`　・　4 欄　・　大約 235 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`name`** 🔑 | text |  |  |  |
| `applied_at` | timestamp with time zone |  | `now()` |  |
| `applied_by` | uuid | ✓ |  |  |
| `source` | text |  | `'recorded'::text` |  |


## `app_secrets`

系統層的金鑰（不是給人看的那本）

主鍵 `name`　・　3 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`name`** 🔑 | text |  |  |  |
| `value` | text |  |  |  |
| `updated_at` | timestamp with time zone |  | `clock_timestamp()` |  |


---

# 系統 ② 公告與董事會

公告、會議、表單、帳密

## `announcements`

公告

主鍵 `id`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `title` | text |  |  |  |
| `body` | text |  |  |  |
| `pinned` | boolean |  | `false` |  |
| `active` | boolean |  | `true` |  |
| `created_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |

索引：

* `ann_list_idx` — `btree (pinned DESC, created_at DESC) WHERE active`


## `announcement_reads`

誰讀過哪一則公告

主鍵 `ann_id, user_id`　・　3 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`ann_id`** 🔑 | uuid |  |  | → `announcements.id` (CASCADE) |
| **`user_id`** 🔑 | uuid |  |  | → `profiles.id` (CASCADE) |
| `read_at` | timestamp with time zone |  | `now()` |  |


## `board_events`

董事會／會議

主鍵 `id`　・　9 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `kind` | text |  | `'meeting'::text` |  |
| `title` | text |  |  |  |
| `starts_at` | timestamp with time zone |  |  |  |
| `all_day` | boolean |  | `false` |  |
| `note` | text | ✓ |  |  |
| `created_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `uploads_open` | boolean |  | `false` |  |

索引：

* `board_events_starts_idx` — `btree (starts_at DESC)`


## `board_files`

會議的附件

主鍵 `id`　・　8 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `event_id` | uuid |  |  | → `board_events.id` (CASCADE) |
| `path` | text |  |  |  |
| `name` | text |  |  |  |
| `size_bytes` | bigint | ✓ |  |  |
| `uploaded_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `author_id` | uuid | ✓ |  | → `profiles.id` (SET NULL) |

索引：

* `board_files_event_idx` — `btree (event_id, created_at)`


## `board_forms`

表單下載區

主鍵 `id`　・　12 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `title` | text |  |  |  |
| `category` | text |  | `'其他'::text` |  |
| `note` | text | ✓ |  |  |
| `file_path` | text | ✓ |  |  |
| `file_name` | text | ✓ |  |  |
| `file_size` | bigint | ✓ |  |  |
| `created_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `updated_at` | timestamp with time zone |  | `clock_timestamp()` |  |
| `usage_note` | text | ✓ |  |  |

索引：

* `board_forms_cat_idx` — `btree (category, title)`


## `board_secrets`

帳密本（受限角色才讀得到）

主鍵 `id`　・　10 欄　・　大約 33 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `title` | text |  |  |  |
| `account` | text | ✓ |  |  |
| `secret` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `created_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_by` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `updated_at` | timestamp with time zone |  | `clock_timestamp()` |  |
| `url` | text | ✓ |  |  |


## `board_secret_reads`

誰在什麼時候看了哪一筆帳密

主鍵 `id`　・　4 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `secret_id` | uuid |  |  | → `board_secrets.id` (CASCADE) |
| `user_id` | uuid | ✓ |  | → `profiles.id` (SET NULL) |
| `read_at` | timestamp with time zone |  | `clock_timestamp()` |  |

索引：

* `board_secret_reads_secret_idx` — `btree (secret_id, read_at DESC)`


---

# 外部資料與行銷

Airbnb 同步、社群、標案

## `airbnb_snapshots`

Airbnb 爬回來的原始快照（進訂單之前的中繼站）

主鍵 `code`　・　18 欄　・　大約 578 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`code`** 🔑 | text |  |  |  |
| `listing_id` | text | ✓ |  |  |
| `guest` | text | ✓ |  |  |
| `start_date` | date | ✓ |  |  |
| `end_date` | date | ✓ |  |  |
| `nights` | integer | ✓ |  |  |
| `status_key` | text | ✓ |  |  |
| `earnings` | numeric | ✓ |  |  |
| `cohost` | numeric | ✓ |  |  |
| `revenue` | numeric | ✓ |  |  |
| `first_seen` | timestamp with time zone |  | `now()` |  |
| `last_seen` | timestamp with time zone |  | `now()` |  |
| `changed_at` | timestamp with time zone | ✓ |  |  |
| `change_note` | text | ✓ |  |  |
| `missing_since` | timestamp with time zone | ✓ |  |  |
| `raw` | jsonb | ✓ |  |  |
| `seen_count` | integer |  | `1` |  |
| `miss_streak` | integer |  | `0` |  |

索引：

* `idx_airbnb_snapshots_changed` — `btree (changed_at DESC NULLS LAST)`
* `idx_airbnb_snapshots_missing` — `btree (missing_since) WHERE (missing_since IS NOT NULL)`
* `idx_airbnb_snapshots_start` — `btree (start_date)`


## `sync_issues`

同步時發現的**待處理**差異（`dismissed_at` 是按掉）

主鍵 `kind, code, field`　・　15 欄　・　大約 14 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`kind`** 🔑 | text |  |  |  |
| **`code`** 🔑 | text |  |  |  |
| **`field`** 🔑 | text |  |  |  |
| `first_seen` | timestamp with time zone |  | `now()` |  |
| `last_seen` | timestamp with time zone |  | `now()` |  |
| `from_val` | text | ✓ |  |  |
| `to_val` | text | ✓ |  |  |
| `listing_id` | text | ✓ |  |  |
| `extra` | jsonb |  | `'{}'::jsonb` |  |
| `severity` | text |  | `'mid'::text` |  |
| `reason` | text | ✓ |  |  |
| `airbnb_changed` | boolean |  | `false` |  |
| `dismissed_at` | timestamp with time zone | ✓ |  |  |
| `dismissed_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |
| `dismissed_sig` | text | ✓ |  |  |

索引：

* `idx_sync_issues_field` — `btree (kind, field)`
* `idx_sync_issues_listing` — `btree (listing_id)`
* `idx_sync_issues_open` — `btree (kind, severity) WHERE (dismissed_at IS NULL)`
* `idx_sync_issues_severity` — `btree (kind, severity, airbnb_changed DESC)`


## `sync_issue_log`

已經處理掉的差異

主鍵 `id`　・　12 欄　・　大約 113 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | bigint |  | `nextval('sync_issue_log_id_seq'::regclass)` |  |
| `kind` | text |  |  |  |
| `code` | text |  |  |  |
| `field` | text |  |  |  |
| `from_val` | text | ✓ |  |  |
| `to_val` | text | ✓ |  |  |
| `reason` | text | ✓ |  |  |
| `severity` | text | ✓ |  |  |
| `opened_at` | timestamp with time zone |  |  |  |
| `closed_at` | timestamp with time zone |  | `now()` |  |
| `resolution` | text |  |  |  |
| `acted_by` | uuid | ✓ |  | → `profiles.id` (NO ACTION) |

索引：

* `idx_sync_issue_log_closed` — `btree (closed_at DESC)`
* `idx_sync_issue_log_code` — `btree (code)`


## `sync_runs`

每一次同步跑了什麼（收到幾筆、新增幾筆…）

主鍵 `id`　・　11 欄　・　大約 141 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | bigint |  | `nextval('sync_runs_id_seq'::regclass)` |  |
| `at` | timestamp with time zone |  | `now()` |  |
| `kind` | text |  |  |  |
| `received` | integer |  | `0` |  |
| `inserted` | integer |  | `0` |  |
| `updated` | integer |  | `0` |  |
| `voided` | integer |  | `0` |  |
| `skipped` | integer |  | `0` |  |
| `detail` | jsonb |  | `'{}'::jsonb` |  |
| `scan_from` | date | ✓ |  |  |
| `scan_to` | date | ✓ |  |  |

索引：

* `idx_sync_runs_at` — `btree (at DESC)`


## `sync_state`

同步的游標與狀態

主鍵 `key`　・　3 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`key`** 🔑 | text |  |  |  |
| `value` | jsonb |  | `'{}'::jsonb` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |


## `social_accounts`

社群帳號（IG／FB）

主鍵 `id`　・　14 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `name` | text |  |  |  |
| `handle` | text |  |  |  |
| `bio` | text | ✓ |  |  |
| `sort` | integer |  | `0` |  |
| `active` | boolean |  | `true` |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ | `auth.uid()` |  |
| `avatar_path` | text | ✓ |  |  |
| `followers` | text | ✓ |  |  |
| `following` | text | ✓ |  |  |
| `platform` | text |  | `'ig'::text` |  |
| `cover_path` | text | ✓ |  |  |
| `category` | text | ✓ |  |  |

索引：

* **UNIQUE** `social_accounts_handle_uidx` — `btree (lower(handle))`


## `social_posts`

貼文排程與草稿

主鍵 `id`　・　15 欄　・　大約 18 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `account_id` | uuid |  |  | → `social_accounts.id` (CASCADE) |
| `sort` | integer |  | `0` |  |
| `caption` | text |  | `''::text` |  |
| `image_path` | text | ✓ |  |  |
| `planned_on` | date | ✓ |  |  |
| `published_on` | date | ✓ |  |  |
| `status` | text |  | `'draft'::text` |  |
| `pin` | integer |  | `0` |  |
| `split_id` | uuid | ✓ |  | → `social_splits.id` (CASCADE) |
| `split_index` | integer | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ | `auth.uid()` |  |
| `deleted_at` | timestamp with time zone | ✓ |  |  |
| `images` | text[] |  | `'{}'::text[]` |  |

索引：

* `social_posts_acc_idx` — `btree (account_id, sort) WHERE (deleted_at IS NULL)`
* **UNIQUE** `social_posts_split_uidx` — `btree (split_id, split_index) WHERE ((split_id IS NOT NULL) AND (deleted_at IS NULL))`


## `social_splits`

一張大圖切成輪播多圖的來源

主鍵 `id`　・　6 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `account_id` | uuid |  |  | → `social_accounts.id` (CASCADE) |
| `source_path` | text | ✓ |  |  |
| `span` | integer |  |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `created_by` | uuid | ✓ | `auth.uid()` |  |


## `tender_feed`

標案爬蟲抓回來的清單

主鍵 `id`　・　11 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `source` | text |  |  |  |
| `title` | text |  |  |  |
| `url` | text |  | `''::text` |  |
| `agency` | text | ✓ |  |  |
| `posted_on` | text | ✓ |  |  |
| `run_at` | timestamp with time zone |  | `now()` |  |
| `starred_at` | timestamp with time zone | ✓ |  |  |
| `starred_by` | uuid | ✓ |  |  |
| `tender_id` | uuid | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |

索引：

* `tender_feed_open_idx` — `btree (run_at DESC) WHERE (tender_id IS NULL)`
* `tender_feed_run_idx` — `btree (run_at DESC)`
* **UNIQUE** `tender_feed_uniq` — `btree (source, url, title)`


## `tenders`

追蹤中的標案

主鍵 `id`　・　12 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  | `gen_random_uuid()` |  |
| `name` | text |  |  |  |
| `source` | text | ✓ |  |  |
| `url` | text | ✓ |  |  |
| `address` | text | ✓ |  |  |
| `due_on` | date | ✓ |  |  |
| `status` | text |  | `'watching'::text` |  |
| `note` | text | ✓ |  |  |
| `feed_id` | uuid | ✓ |  | → `tender_feed.id` (SET NULL) |
| `created_by` | uuid | ✓ |  |  |
| `created_at` | timestamp with time zone |  | `now()` |  |
| `updated_at` | timestamp with time zone |  | `now()` |  |

索引：

* `tenders_due_idx` — `btree (due_on)`


---

# 備份／一次性

**沒有任何程式讀這幾張。**

## `bank_statements_backup_144`

`migration_144` 的備份

**沒有主鍵**　・　17 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| `id` | uuid | ✓ |  |  |
| `account_id` | uuid | ✓ |  |  |
| `period_from` | date | ✓ |  |  |
| `period_to` | date | ✓ |  |  |
| `closing_balance` | numeric(14,2) | ✓ |  |  |
| `total_debit` | numeric(14,2) | ✓ |  |  |
| `total_credit` | numeric(14,2) | ✓ |  |  |
| `parsed_count` | integer | ✓ |  |  |
| `inserted_count` | integer | ✓ |  |  |
| `skipped_count` | integer | ✓ |  |  |
| `warnings` | text[] | ✓ |  |  |
| `file_name` | text | ✓ |  |  |
| `uploaded_by` | uuid | ✓ |  |  |
| `uploaded_at` | timestamp with time zone | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `account_name` | text | ✓ |  |  |
| `txn_count` | bigint | ✓ |  |  |


## `bank_statements_last_wipe`

清空對帳單前的備份

**沒有主鍵**　・　18 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| `id` | uuid | ✓ |  |  |
| `account_id` | uuid | ✓ |  |  |
| `period_from` | date | ✓ |  |  |
| `period_to` | date | ✓ |  |  |
| `closing_balance` | numeric(14,2) | ✓ |  |  |
| `total_debit` | numeric(14,2) | ✓ |  |  |
| `total_credit` | numeric(14,2) | ✓ |  |  |
| `parsed_count` | integer | ✓ |  |  |
| `inserted_count` | integer | ✓ |  |  |
| `skipped_count` | integer | ✓ |  |  |
| `warnings` | text[] | ✓ |  |  |
| `file_name` | text | ✓ |  |  |
| `uploaded_by` | uuid | ✓ |  |  |
| `uploaded_at` | timestamp with time zone | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `account_name` | text | ✓ |  |  |
| `txn_count` | bigint | ✓ |  |  |
| `wiped_at` | timestamp with time zone | ✓ |  |  |


## `deleted_contract_orders_81`

`migration_81` 刪掉的月租單

主鍵 `id`　・　10 欄　・　大約 757 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| **`id`** 🔑 | uuid |  |  |  |
| `contract_id` | uuid |  |  |  |
| `guest_name` | text | ✓ |  |  |
| `order_key` | text | ✓ |  |  |
| `checkin` | date | ✓ |  |  |
| `amount` | numeric | ✓ |  |  |
| `paid` | boolean | ✓ |  |  |
| `imported_via` | text | ✓ |  |  |
| `row_json` | jsonb |  |  |  |
| `deleted_at` | timestamp with time zone |  | `now()` |  |


## `deleted_stub_orders_93`

`migration_93` 刪掉的空殼訂單

**沒有主鍵**　・　38 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| `id` | uuid | ✓ |  |  |
| `order_key` | text | ✓ |  |  |
| `source` | text | ✓ |  |  |
| `estate_id` | uuid | ✓ |  |  |
| `property_id` | uuid | ✓ |  |  |
| `property_raw` | text | ✓ |  |  |
| `guest_name` | text | ✓ |  |  |
| `checkin` | date | ✓ |  |  |
| `checkout` | date | ✓ |  |  |
| `nights` | integer | ✓ |  |  |
| `amount` | numeric | ✓ |  |  |
| `deposit` | numeric | ✓ |  |  |
| `account` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `contract_id` | uuid | ✓ |  |  |
| `imported_via` | text | ✓ |  |  |
| `created_at` | timestamp with time zone | ✓ |  |  |
| `paid` | boolean | ✓ |  |  |
| `deposit_received` | boolean | ✓ |  |  |
| `deposit_returned` | boolean | ✓ |  |  |
| `parent_order_id` | uuid | ✓ |  |  |
| `fee_type` | text | ✓ |  |  |
| `fx_revenue` | jsonb | ✓ |  |  |
| `fx_deposit` | jsonb | ✓ |  |  |
| `move_group` | uuid | ✓ |  |  |
| `deposit_received_at` | date | ✓ |  |  |
| `deposit_returned_at` | date | ✓ |  |  |
| `paid_at` | date | ✓ |  |  |
| `item_name` | text | ✓ |  |  |
| `paid_amount` | numeric | ✓ |  |  |
| `invoice_required` | boolean | ✓ |  |  |
| `invoice_title` | text | ✓ |  |  |
| `invoice_tax_id` | text | ✓ |  |  |
| `account_code` | text | ✓ |  |  |
| `start_date` | date | ✓ |  |  |
| `end_date` | date | ✓ |  |  |
| `cadence` | text | ✓ |  |  |
| `backed_up_at` | timestamp with time zone | ✓ |  |  |


## `deposits_fix_b6_b5`

押金 B6→B5 移房前的備份

**沒有主鍵**　・　39 欄　・　大約 0 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| `id` | uuid | ✓ |  |  |
| `order_id` | uuid | ✓ |  |  |
| `contract_id` | uuid | ✓ |  |  |
| `estate_id` | uuid | ✓ |  |  |
| `property_id` | uuid | ✓ |  |  |
| `room` | text | ✓ |  |  |
| `guest_name` | text | ✓ |  |  |
| `currency` | text | ✓ |  |  |
| `amount` | numeric | ✓ |  |  |
| `received_on` | date | ✓ |  |  |
| `received_method` | text | ✓ |  |  |
| `received_account` | text | ✓ |  |  |
| `returned_on` | date | ✓ |  |  |
| `returned_method` | text | ✓ |  |  |
| `returned_account` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `orphaned` | boolean | ✓ |  |  |
| `created_at` | timestamp with time zone | ✓ |  |  |
| `is_manual` | boolean | ✓ |  |  |
| `refund_status` | text | ✓ |  |  |
| `payee_bank_code` | text | ✓ |  |  |
| `payee_name` | text | ✓ |  |  |
| `payee_account` | text | ✓ |  |  |
| `planned_refund_on` | date | ✓ |  |  |
| `refund_requested_by` | uuid | ✓ |  |  |
| `refund_requested_at` | timestamp with time zone | ✓ |  |  |
| `manager_approved_by` | uuid | ✓ |  |  |
| `manager_approved_at` | timestamp with time zone | ✓ |  |  |
| `admin_approved_by` | uuid | ✓ |  |  |
| `admin_approved_at` | timestamp with time zone | ✓ |  |  |
| `rejected_by` | uuid | ✓ |  |  |
| `rejected_at` | timestamp with time zone | ✓ |  |  |
| `reject_reason` | text | ✓ |  |  |
| `lines` | jsonb | ✓ |  |  |
| `transfer_to_id` | uuid | ✓ |  |  |
| `transfer_from_id` | uuid | ✓ |  |  |
| `transferred_by` | uuid | ✓ |  |  |
| `transferred_at` | timestamp with time zone | ✓ |  |  |
| `_backup_at` | timestamp with time zone | ✓ |  |  |


## `orders_backup_136`

`migration_136` 的備份

**沒有主鍵**　・　35 欄　・　大約 2,681 列

| 欄位 | 型別 | 可空 | 預設 | 外鍵 |
|---|---|---|---|---|
| `id` | uuid | ✓ |  |  |
| `order_key` | text | ✓ |  |  |
| `source` | text | ✓ |  |  |
| `estate_id` | uuid | ✓ |  |  |
| `property_id` | uuid | ✓ |  |  |
| `property_raw` | text | ✓ |  |  |
| `guest_name` | text | ✓ |  |  |
| `checkin` | date | ✓ |  |  |
| `checkout` | date | ✓ |  |  |
| `nights` | integer | ✓ |  |  |
| `amount` | numeric | ✓ |  |  |
| `deposit` | numeric | ✓ |  |  |
| `account` | text | ✓ |  |  |
| `note` | text | ✓ |  |  |
| `contract_id` | uuid | ✓ |  |  |
| `imported_via` | text | ✓ |  |  |
| `created_at` | timestamp with time zone | ✓ |  |  |
| `paid` | boolean | ✓ |  |  |
| `deposit_received` | boolean | ✓ |  |  |
| `deposit_returned` | boolean | ✓ |  |  |
| `parent_order_id` | uuid | ✓ |  |  |
| `fee_type` | text | ✓ |  |  |
| `fx_revenue` | jsonb | ✓ |  |  |
| `fx_deposit` | jsonb | ✓ |  |  |
| `move_group` | uuid | ✓ |  |  |
| `deposit_received_at` | date | ✓ |  |  |
| `deposit_returned_at` | date | ✓ |  |  |
| `paid_at` | date | ✓ |  |  |
| `item_name` | text | ✓ |  |  |
| `paid_amount` | numeric | ✓ |  |  |
| `invoice_required` | boolean | ✓ |  |  |
| `invoice_title` | text | ✓ |  |  |
| `invoice_tax_id` | text | ✓ |  |  |
| `account_code` | text | ✓ |  |  |
| `backed_up_at` | timestamp with time zone | ✓ |  |  |


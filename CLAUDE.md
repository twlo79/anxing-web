# 給 Claude 的工作說明

## 回覆格式（David 指定，2026-08-15）

每次回覆固定四段，**簡潔、明確**：

### 1. 我要做什麼

編號步驟，含可以直接複製的 code。不要只說「去某某頁面改」——寫出頁面路徑、欄位名稱、要按哪顆按鈕。

### 2. Claude 做了什麼

改了哪些檔案、為什麼這樣改、要 review 什麼。**風險與取捨要講在前面**，不要藏在最後一句。

### 3. 其他優化建議

看到但這次沒做的。沒有就寫「沒有」，不要硬湊。

### 4. 驗證

tsc、測試、migration 自檢的結果。

### 4.5 migration 一律附可點開的檔案卡

寫了 `.sql` 就用 `present_files` 給卡片，**不要只講檔名**。
「supabase/migrations/migration_185_xxx.sql」他得自己去資料夾裡找 ——
而那個資料夾有十幾支長得很像的檔案（2026-08-31 連兩次「找不到 183」「找不到 185」）。

### 5. 推送（有要推的時候才給）

**指令**：連 `cd` 與 commit 訊息一起給，整段複製貼進 PowerShell 就跑完 ——
不要讓他還要回答一次「commit 訊息」。

```powershell
cd C:\Users\ASUS\Desktop\anxing-web
.\deploy.ps1 "效能：身分全站只查一次、比較期不排隊"
```

commit 訊息一行，中文，動詞開頭或「主題：內容」。

**note**：條列這次改了什麼、有沒有 migration 要跑、行為有沒有變。
寫給三個月後的自己看 —— 那時 git log 只剩一行標題，細節全靠這裡。

---

## ★★★ UI 改版一律先過審（David 指定，2026-08-29）

**動到畫面長相的事，先給看、等一句「做」，才可以改檔案。**

適用範圍 —— 只要使用者會看到差別就算：

* 版面配置（卡片怎麼排、寬高、間距、一行放幾個）
* 字級、顏色、圓角、邊框
* 元件換掉（手寫的換成共用元件、A 元件換成 B 元件）
* 欄位或按鈕的**文字**、增刪、移位
* 拿掉既有行為（捲軸、截斷、即時篩選）

**怎麼給看**：畫出來（`show_widget` 的 HTML 稿）或截圖標註，
**不要用文字描述版面** —— 「卡片會變高一點」跟實際看到的差很多。
超過一種做法時給 A／B 並排，講清楚差在哪、各自的代價，讓他挑。

**過審之前**：`tsc`、測試、把邏輯抽到 `.ts` 這些不動長相的準備工作可以先做。

**不用過審**：純 bug 修（畫面本來就壞的）、他這一則訊息裡已經指定到具體樣式的
（「改叫收支」「星星旁邊加 i」）。

★ 這一條是因為 2026-08-29 那次連續改了七頁的分項面板才加的 ——
改完才發現版面不好看，而那時已經沒有一個乾淨的還原點。

---

## ★★★ 畫面上的文字與版面（David 指定，2026-09-02）

### 1. 說明文字簡練清楚

一句話講完。**寫給不知道前因後果的人看**，不要解釋實作。

* 好：「沒填房源的話，要填間數或打掃點數」
* 壞：「因為 filterItems 會過濾掉沒有 property_code 的項目，所以⋯」

長度上限：提示一行、警告兩行。超過就是這件事的設計有問題，不是文案問題。

### 2. 統一用語 —— 同一件事只准有一個名字

畫面上、註解裡、跟 David 講話時都用這一欄：

| 用這個 | 不要用 |
|---|---|
| 暫付 | 暫支、暫支款、預付 |
| 補登 | 手動補、手動加入、補建 |
| 間數 | 打掃量、房間數 |
| 打掃點數 | 清潔點數、點數（單獨用） |
| 間數（＝這份工算幾間）| 工作量、數量 |
| 金額（＝這份工付多少，覆寫公式）| 單價、費用 |
| 房源 | 房間、物件 |
| 物業 | 建案、案場 |
| 出款 | 付款、匯出 |
| 按掉 | 忽略、略過、關閉 |
| 實支 | 真的付款、已付現 |
| 非實支 | 房務、內部成本、應計未付 |
| 用途（＝物業或安幸辦公室）| 物業欄、歸屬 |
| 項目（＝這筆在做什麼）| 用途、說明、摘要 |
| 工單（＝排班表上一格:誰、哪天、哪個房源、做什麼）| 工作項目、一份工、打掃紀錄 |
| 職位（＝`staff.staff_type`:管家／房務／經理／會計／總經理）| 身分、角色 |
| 權限（＝`profiles.role`:管家／房務／主管／會計／總經理）| 角色、role |
| 物業負責人（＝`estate_managers` 那個時間點負責的人，可能是管家也可能是經理）| 管家、負責管家 |
| 拆帳（＝一份工的錢記到好幾間）| 分攤、拆單、拆開 |
| 取用（＝備品從櫃子裡拿走，−）| 領用、出庫、消耗 |
| 補貨（＝備品放進櫃子，＋）| 入庫、進貨、補充 |
| 盤點（＝月底數實際有多少）| 清點、對帳、校正 |
| 餘量（＝備品現在還剩多少，**算出來的**）| 庫存、結存、現有數量 |

★ 新的名詞先加進這張表再用。同一個東西在兩頁叫不同名字，
使用者會以為那是兩件事 —— 而那不會有人來報，只會變成「這系統很難用」。

### 3. 警示與說明不可以推歪版面

提示出現或消失時，**旁邊的欄位一格都不准動**。

* 一排 `items-end` 的欄位，底下多一行字就會把那一格的輸入框往上推
* 修法：`relative` 的父層 ＋ `absolute top-full` 的提示（不佔高度）
* 或者一開始就留好那一行的位置

★ 2026-09-02 踩過:補登表單打了主檔沒有的房源，整排欄位錯開一階。
使用者看到的是「畫面壞了」，而那只是一行提示。

---

## 這個專案的規矩

* **不能執行 `deploy.ps1`**（sandbox 的 `next build` 會在 SWC 原生檔掛掉）。也**不用 raw git 幫 David 先 commit** —— 推是他的動作。
* **migration 是手動貼進 Supabase SQL Editor 的**，CI 不碰。每支結尾要有回傳結果的自檢表格（`raise notice` 在 SQL Editor 看不到）。
* **邏輯寫在 `.ts` 不是 `.tsx`** —— 測試執行環境不處理 JSX，寫在 `.tsx` 裡的判斷式測不到。
* 改完跑：

```bash
npx tsc --noEmit --skipLibCheck
node --experimental-strip-types --test "src/**/*.test.ts"
```

## 幾個一定要記得的坑

| 坑 | 症狀 |
|---|---|
| Supabase 預設最多回 **1000 列且不報錯** | 要加總的查詢必須分頁，不然第 1001 筆之後安靜消失 |
| RLS 擋下的 UPDATE/DELETE **回成功且影響 0 列** | 要檢查 `.select('id')` 長度，或走 SECURITY DEFINER RPC |
| `now()` 是**交易開始時間** | 同一交易裡呼叫兩次會相同，要用 `clock_timestamp()` |
| `information_schema.columns` 沒帶 `table_schema='public'` | `properties` 這種名字在其他 schema 也有，自檢會誤報 |
| SQL Editor 把整份腳本包在**一個交易**裡 | 任一錯誤全部回滾；會失敗的東西（建索引、裝擴充）要用 exception 包起來 |
| PostgREST 批次 upsert 取**欄位聯集** | 某列少了鍵會被填 null，必須明寫回舊值 |
| **`.ps1` 存成沒有 BOM 的 UTF-8** | Windows PowerShell 5.1 當成 Big5 讀 —— 中文全變亂碼，而亂碼裡湊出引號就**整支腳本語法錯誤**。所有 `.ps1` 開頭三個 byte 必須是 `EF BB BF`（2026-08-31 踩過）|
| **傳給 `.exe` 的路徑有中文** | PS 5.1 呼叫原生程式時用 ANSI codepage 轉參數 → `.exe` 收到亂碼路徑，回「No such file or directory」**而那個資料夾確實存在**。任何要交給外部程式的路徑一律純英數字（2026-08-31 踩過）|
| **`\|\|` 接系統目錄的欄位** | `confdeltype`、`contype` 那些是 `"char"` 不是 text → `operator is not unique`。自檢裡拼字串一律明寫 `::text`（2026-09-01 踩過）|
| **`pg_get_functiondef()` 掃整個 `pg_proc`** | 掃到聚合函式就 `ERROR: 42809: "array_agg" is an aggregate function`，整支 migration 回滾 —— 而爆掉的是自檢。一律加 `and p.prokind in ('f','p')`（`supabase/dump-schema.sql` 本來就這樣寫，2026-09-01 沒照抄而踩到）|
| **自檢沒涵蓋既有資料的合法形狀** | 誤報會把真警報淹掉。194 的「amount vs lines」把「lines 是空的」也算成不一致 —— 6 筆正常資料被標紅，**真正壞掉的那 1 筆埋在裡面差點被當成雜訊**。寫檢查前先問「這個欄位有幾種合法的樣子」（2026-09-01 踩過）|
| **同一條規則在三個地方各寫一次** | 「誰可以被指派房源工作」寫在 `hkParse`、排班表 inline、例外補登 —— 前兩個 `!== 'none'`、第三個 `=== 'rooms'`。結果:匯入會給劉姐建工作，手動補卻選不到她。**兩條路對同一個人給出不同答案，畫面上完全看不出來**（2026-09-01 踩過）|
| **一份資料存在兩個地方** | `deposits.amount` 與 `deposits.lines` 各存一次台幣金額。契約那兩支同步函式只寫 amount，改金額後 lines 留在原地 —— 不報錯、總額正常、只有畫面上的數字是舊的。修法不是對帳，是讓寫的人只有一個（migration_195）|
| **`schema-baseline.sql` 不等於線上** | 那份是舊的 dump。實際的 policy 比它多（`hk_task` 整張表、五條 `_read`、`hk_work_type_write` 都不在裡面）。照它抄表名 → 漏掉的東西**不會叫**，自檢還會回「✅ 11/11」因為只問了抄來的那 11 條。掃 `like 'hk\_%'` 不要列表名；`drop policy` 更是絕對不行（會拔掉沒看過的那五條，症狀是房務阿姨什麼都看不到）（2026-09-02 踩過）|
| **條件寫死 `kind = 'cash'` 而不是 `manual_entry`** | migration_187 重算現金餘額時 `where a.kind = 'cash'`；192 後來把 08311 改成手動記帳，但 `kind` 還是 `bank` → **掃不到**。症狀是 08311 三筆存入共 $51,083、每一列餘額都顯示 $0，而沒有任何錯誤。192 自己的檔頭就警告過「那個 `or` 會被複製到十個地方」—— 它被複製到的第十一個地方是比它更早的 187（2026-09-02 踩過，migration_203 修）|
| **新建表沒跟著寫 RLS policy** | Supabase 的 `rls_auto_enable()` 會**自動幫新表開 RLS**，而沒有 policy 等於全部擋掉 —— 查詢**回成功、0 列**。症狀是畫面上一個很正常的「0 筆 $0」，沒有任何錯誤。migration_206 建 `hk_labor_cost` 漏了 policy，六筆人事費前端一筆都讀不到（2026-09-02 踩過，208 修）。`create table` 之後**一定**接 `enable row level security` ＋ `create policy` |
| **推導值存成欄位** | `bank_transactions.balance` 是算出來的卻存進欄位，維護它的只有前端一段程式。第二個寫入者、或那條路徑失敗一次，它就永遠是錯的**而且不會叫**。修法是讓資料庫用觸發器維護（migration_203），不是對帳 |
| **兩個格式不同的字串拿去比對** | `'2026-08-20'.startsWith('202608')` 永遠是 false（日期有橫線、period 沒有）—— 那個守衛把**每一次**補登都擋掉。tsc 過、沒有測試、畫面上看起來像權限問題。使用者連報三次「不能存」，前兩次我都在查別的東西（2026-09-02 踩過，`inPeriod()` 修）|
| **錯誤訊息跳在頁面最上方** | 而使用者在畫面下半部操作，捲下來就看不到。他按了鈕、什麼都沒發生，結論是「按鈕壞了」—— 而系統其實有講原因。**訊息要出現在動作發生的地方**（2026-09-02 踩過）|
| **拿兩個問法不同的檢查互相比較** | 「餘額剛好是 0」抓到 1 筆，「餘額跟重算不一樣」抓到 18 筆 —— 我拿前者的數字去預測後者，於是跟使用者說「不是 4 就要停下來」。**比數字之前先問兩邊問的是不是同一件事**（2026-09-02 踩過）|
| **自檢的母體是空的 → 每一條都回綠** | migration_210 的六列自檢全部 ✅，而第 1 列「房務支出總筆數」是 **0** —— 一筆都沒產生過，所以「還有幾筆沒補」「有沒有錯置」通通自動成立。**紅字會讓人停下來，六個綠勾讓人以為做完了。**<br>★ 我把母體數寫成「參考值，不判斷對錯」，等於自己把唯一的警報關掉。母體筆數要**判定**:是 0 就寫「⚠ 沒有東西可檢查，下面全部不算數」（2026-09-03 踩過）|
| **憑印象寫既有函式的簽章** | `record_migration` 是 `(p_name text)` 一個參數、名字不帶 `migration_` 前綴，而我寫成 `(210, 'migration_210_xxx', '說明')` → `function does not exist`，整支在 commit 前炸掉。200~209 每一支都是照 `to_regprocedure('public.record_migration(text)')` 包起來的固定寫法 —— **上一支怎麼呼叫就怎麼抄，不要憑印象**。同一輪還連帶寫錯 `schema_migrations` 的欄位（是 `name` 不是 `version`）。跟 2026-09-01 那次 `pg_get_functiondef()` 沒照抄 `dump-schema.sql` 同一種錯（2026-09-03 踩過）|
| **`.upsert()` / `.update()` 的回傳值沒接** | `setDay()` 寫 `hk_day.rooms_override`，而那一欄**線上根本不存在**（migration_60 寫了但沒生效）。前端先做樂觀更新、又沒接 `error` → 畫面上數字有變、重新整理跳回原值。使用者三個月來以為是「系統會自己算回去」。<br>★ 讀那邊是 `select('*')`，欄位不存在只是 `undefined`，一樣不叫。**一個靜默的讀 ＋ 一個靜默的寫 ＝ 一個不存在的功能**。<br>★★ 失敗時要把樂觀更新**收回去**，不是只跳訊息 —— 留著假數字比沒改更糟（2026-09-03 踩過，migration_211 修）|
| **沒去看欄位定義就決定「空值長什麼樣」** | `expenses.tags` 是 `text[] not null default '{}'`，而我讓存檔寫 `null`，理由是「讓沒有標籤只有一種形狀」。方向對，但**那個形狀資料庫早就選好了**。結果是整個支出頁存不了檔 —— 使用者第一個動作（改個用途）就撞上 `violates not-null constraint`。<br>★ 要統一空值的形狀之前，先 `grep` 那一欄的 `create/alter table`。tsc 不會抓（型別是 `string[] \| null`），測試也不會抓（我自己把「回 null」寫成了預期值）（2026-09-03 踩過）|
| **編輯用的欄位放在會消失的地方** | 房務的「間數」只在例外分頁的補登表單裡，而那個表單的入口是「沒進系統的」清單**每一列上**的按鈕。使用者把 5 筆都按掉之後入口消失 —— 於是沒有任何一條路改得了間數，而程式碼裡那個表單好端端地在。<br>★ 程式自己的註解寫著「房源格是唯一真實來源、唯一可以新增/刪除的輸入點」，但間數欄不在那裡。**註解說的唯一入口，跟實際的欄位分佈不一致**。<br>★★ 判斷法:一個欄位如果只在「處理例外」的畫面上填得到，那它在例外清空之後就沒人改得動了（2026-09-03 踩過）|
| **只找到一條產生路徑就當成唯一的** | 月租單有**兩支**產生器:有房號的走資料庫觸發器 `gen_contract_orders()`（`LT_{房號}_{YYYYMM}`），**沒房號的（公司登記／辦公室登記）走前端** `lib/ltKey.keyBase`（`LTC_{契約id}_{YYYYMM}`，migration_77）。我只讀了 `schema-baseline.sql` 裡那支，看到「`room is null` 就 return」就判定 28 張公司登記「一張月租單都不會產生」—— 而它們每一張都有。<br>★ `schema-baseline.sql` 那條坑（2026-09-02）的另一種形狀:**不只是舊，是它根本不含前端那一半**。判定「某類資料不存在」之前，先 grep 那個鍵的字首（2026-09-03 踩過）|
| **判定照程式碼寫，沒拿同一列的資料對** | 上面那次，我印出的同一列寫著「未收合計 148,526」—— 一張月租單都沒有的話那個數字不可能存在。**證據就在我自己印的那一行，而我沒讀。** 判定欄要用實際筆數算（`count(*) = 0`），不要用「我讀程式碼推出來的原因」算（2026-09-03 踩過）|
| **職位（staff_type）與權限（role）混成同一個字** | 清潔記錄頁把職位 `manager` 印成「主管」，但「主管」是**權限**的名字，職位叫「經理」。權限管理頁上這兩欄本來就是分開的兩欄 —— 只有那一頁混在一起，於是同一個人在兩頁看到兩個頭銜，使用者會以為那是兩件事（2026-09-03 踩過）|
| **「誰可以做這件事」寫成職位的等號比對** | 「接手的管家」下拉寫 `staff_type === 'housekeeper'` → 經理兼管家時**選不進去**，她管的物業評價歸不到任何人，而畫面上只是負責人欄空著，沒有地方會叫。同一輪還有 `shareRec` 的 `isHk` 把她的管家檢查發成「房務員」通知 —— **一個 `===` 會散在好幾個地方**，改的時候要 grep 那個字串而不是只改看得到的那一個（2026-09-03 踩過）|
| **同一支欄位既拿來顯示又拿來當 key** | `Unpriced.label` 空的時候顯示成「（沒填房源）」，而畫面若拿那四個字去存 `job_code`，`splitJobKey` 就對不上（它用原始的空字串）。症狀是「拆完存好了，但支出完全沒變」—— tsc 過、沒有錯誤訊息、使用者只會再拆一次。**顯示的代換一律留在畫面那一層**；要當 key 的欄位另外開一支原始值（2026-09-03 寫拆帳時抓到，跟 `'2026-08-20'.startsWith('202608')` 同一種病）|
| **整批刪掉重建有 id 的東西** | 支出的冪等鍵是 `split:<拆帳列的 id>`。存拆帳時圖方便「全刪再全建」的話 id 全變 → 已產生的支出**全部變孤兒**，下次按產生又多一整組，**帳直接雙倍**而總額只是「比較大」。改法:留著的 update（id 不動）、新的 insert、拿掉的才 delete，每一步數影響列數 |
| **自檢的基準值依賴這支正在改的東西** | migration_187 拿「最後一筆的餘額」當基準，而那支改的正是順序 —— 基準自己會被改掉。基準要用**跟改動無關**的量（總和、筆數）|
| **★★★ 憑對話紀錄判斷 migration 跑了沒** | 2026-09-05 我連續三次說「213／215／216 未跑」，而三支**都在 09-03 跑完了**。事實在 `schema_migrations`，不在我的摘要裡 —— 對話會被壓縮、會跨 session、會記錯。<br>★ 代價不只是白跑一趟:213 的自檢因此回了一個誤導的 0（見下一條），我花了**兩輪查詢在追一個不存在的 bug**。<br>★★ 規矩:要 David 跑任何一支之前，先請他跑 `select name from schema_migrations where name >= '210' order by name;`，或在那支自己的自檢裡放一列「這支跑過了沒」。**不要說「這支還沒跑」除非剛看過那張表。**<br>★★★ **「推了沒」也一樣**（2026-09-08 又踩一次）:我列了五項「同批未推」，而其中四項 David 早就 commit 了（`git log` 三行就看得到）。事實在 `git status` 與 `git log`，不在我的摘要裡。**要寫「未推」之前先跑一次 `git status --short`。** |
| **★★★ PostgREST 的 `upsert` 對不到 partial 唯一索引** | `expenses_hk_job_uniq` 建成 `unique (hk_job_key) where hk_job_key is not null`，而前端寫 `upsert(rows, { onConflict: 'hk_job_key' })` —— PostgREST 只送得出**欄位名**，表達不出那個 WHERE，Postgres 直接丟 `there is no unique or exclusion constraint matching the ON CONFLICT specification`。<br>★ 結果是**房務支出這個功能從上線到現在一次都沒成功過**（`hk_job_key` 有值的是 0 筆），而使用者只說「沒產生到支出」。<br>★★ 錯誤**有被接到**，但走 `flash()` 跳在頁面最上方、2.5 秒消失，而那個面板在畫面下半部 —— 半年來每一次失敗都有訊息，只是沒有人看得到。<br>★★★ 拿掉 `onConflict` 也不行:PostgREST 沒有那個參數時會拿**主鍵**當衝突目標，而 `id` 每次都是新 uuid —— 永遠不衝突，按兩次就產生兩整組，比失敗更糟。要用 upsert 就**不能用 partial 索引**（唯一索引本來就允許多個 NULL，拿掉 WHERE 語意不變）（2026-09-07 踩到，migration_221 修）|
| **★★ 自檢問「這次跑做了什麼」而不是「結果對不對」** | migration_213 的第 4 列寫成「最近十分鐘有沒有 expenses 的刪除」。那支是冪等的 —— 第二次跑會走「找不到那筆，不做事」直接 return，於是那一格變成 0，看起來像稽核系統壞了。<br>★ 而且「十分鐘內」**會隨時間過期**。一個會自己變紅的檢查等於沒有檢查。<br>★★ 判斷法:把自檢跑第二次、第十次，答案該一模一樣。會變的那一條就是寫錯的那一條（2026-09-05 踩過）|

## 判斷原則

* **對不上的不猜。** 少填一個看得到、補得回來；填錯一個沒有人會發現。
* **建議，不自動。** 系統負責看見，人負責決定（打勾接受／打叉忽略）。
* **寧可晚一天報。** 漏報一筆是晚一天發現；誤報 46 筆是整份清單再也沒有人看。

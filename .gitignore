node_modules
.next
out

# 環境變數(含金鑰,絕不進版控)
.env.local
.env*.local
.env.sync

# 編譯 / 建置產物(next-env.d.ts 例外,Next.js 要求納入版控)
*.tsbuildinfo

# 系統雜檔
.DS_Store
Thumbs.db
*.log

# Supabase CLI 的本機快取(含 project ref、pooler url),不是專案的一部分
supabase/.temp/

# 公司財務檔。不是程式的一部分,而且進了版控就永遠留在歷史裡 ——
# 之後再刪也刪不掉,任何拿得到 repo 的人都翻得出來
*.xlsx

# TimeTree 抓下來的排班原始資料。
#
# 檔名看起來無害,內容是**員工姓名 ＋ 房客姓名**(「退-A2-Martin Kossa」)。
# 那是個資,不該躺在程式碼倉庫裡跟著每一次 clone 複製出去。
#
# 抓取腳本本身留著(sync-housekeeping.ps1)—— 那是程式,要版控。
timetree-sync/*.json
timetree-sync/*.txt

# ── 資料庫備份（2026-08-31）──────────────────────
#
# ★★★ 備份檔裡是**整個資料庫**:房客姓名電話、租金、銀行帳號、
#   員工薪資。commit 進去等於把全部公開,而 git 的歷史刪不乾淨 ——
#   改掉之後舊的 commit 裡還在。
#
# 預設的備份位置是「文件\安幸資料庫備份」(不在專案裡),
# 但萬一有人手動存到專案資料夾,這幾條擋住它。
*.sql.gz
*.dump
anxing_*.sql
backup/
# ★ 資料夾整個擋掉 —— 2026-08-31 那次 pg_dump 因為工作目錄沒同步，
#   把備份寫進了專案資料夾裡（C:\...\anxing-web\anxing-db-backup\）。
#   裡面的檔案被 anxing_*.sql 擋住了，但資料夾本身要一起擋，
#   免得哪天檔名規則改了又漏出去。
anxing-db-backup/
